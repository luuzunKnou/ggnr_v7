# 작업계획서

2026-07-07 | 200BingSu | npm run build 타입 오류 수정

## 개발목표

프로덕션 빌드(`npm run build`) 시 TypeScript 검사 단계에서 발생하던 타입·인터페이스 오류를 제거한다. 런타임 동작 변경 없이 타입 정의·단언·가드만 보정하여 빌드가 정상 완료되도록 한다.

## 플로우차트

빌드 실행 → 타입 오류 목록 확인 → 파일별 수정 → tsc·build 재검증 → 성공

## 화면설계

해당 없음 (서버·지도 내부 타입 보정, 화면 UI 변경 없음)

## 수정추가파일

수정 | src/app/(pages)/map/_mapComponents/MapContext.tsx
  -> 컨텍스트 값 타입에서 null 유니온 분리. Pick 기반 유틸 타입 오류 근본 원인 제거

수정 | src/app/(pages)/map/_mapComponents/layerRowEdit/LayerRowGeomEditHandler.tsx
  -> GeoJSON geometry 단언, 스타일 함수 FeatureLike, 필지 삭제 콜백 매개변수 타입 정합

수정 | src/app/(pages)/map/_mapContents/road/roadLedger/RoadLedgerListPanel.tsx
  -> 지도 인스턴스 null 가드 추가 후 extent 맞춤 호출

수정 | src/lib/mapServiceMenuLayers.ts
  -> 서비스 메뉴 레이어 초기화 대상 타입을 Partial로 완화 (선택적 setter 허용)

수정 | src/service/layerRowService.ts
  -> defineLayer 필드 메타 map·filter 반환 타입을 DefineFieldMeta와 일치

수정 | src/service/orthophotoService.ts
  -> 누락된 데이터 경로 상수 import 추가

수정 | src/service/pdfToJpgService.ts
  -> PDF 변환 결과 변수 타입에 실패 건수·실패 목록 필드 포함
