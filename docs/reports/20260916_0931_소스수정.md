# 결과보고서

2026-09-16 | gyujin | 정사영상 변환 24시간 절대 상한 제거

## 목적

대용량 좌표 변환이 하루를 넘겨도 자동으로 끊기지 않게 해, 장시간 돌린 작업이 중간에 버려지지 않도록 한다.

## 요약

정사영상 GDAL 프로세스의 24시간 절대 상한 종료를 없앴다. 기본은 시간 제한 없이 완료될 때까지 유지하고, 필요 시에만 상한을 따로 줄 수 있다. 합본·좌표 변환·타일링의 무응답 종료는 이전과 같이 꺼져 있다.

## 파일수정

수정 | src/service/orthophotoService.ts
 `runProcess` — 절대 상한 기본값을 없음(0)으로 변경. 양수일 때만 시간 초과로 종료
 `getOrthoMaxTimeoutMs` — 24시간 고정 상한 함수 삭제

## 테이블 수정

해당 없음

## 테스트

1. 좌표 변환을 24시간 이상 돌려도 «절대 상한 초과»로 종료되지 않아야 한다.
2. 변환이 정상 완료되면 이전과 같이 타일 배포까지 이어져야 한다.
3. 배치를 다시 실행해야 새 제한이 적용되어야 한다.

## 프롬프트

—
—
