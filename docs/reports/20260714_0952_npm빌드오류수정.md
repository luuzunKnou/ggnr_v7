# 결과보고서

2026-07-14 | 200BingSu | npm run build TypeScript·지도 Suspense 오류 수정

## 목적

버전관리 재기동 파이프라인의 `npm run build`가 실패하지 않도록 타입 오류와 지도 페이지 정적 생성 오류를 고친다.

## 요약

파일변환 화면의 누락 타입, 필지·업로드·IP·ZIP 관련 타입 불일치, AbortSignal null 처리를 바로잡았다. 지도는 `useSearchParams` 사용처를 Suspense로 감싸 빌드 시 prerender 오류를 없앴다. `npm run build`가 성공한다.

## 파일수정

수정 | src/app/(pages)/dev/_components/FileConverterContent.tsx
 `PdfToJpgJobRow` — 클라이언트용 타입 정의 추가 (미정의 참조 오류 해소)

수정 | src/lib/parcelLandInfoTab.ts
 `hasParcelLandInfoTabData` — source 필수 제약 완화로 탭 데이터 검사 호환

수정 | src/lib/hostMachineIpv4.ts
 IPv4 family 비교 — Node 타입(string)과 구버전(number) 모두 허용

수정 | src/lib/sourceVersionClientRelay.ts
 `fetchWithTimeout` — AbortSignal null을 undefined로 정규화

수정 | src/service/mapAnalyseService.ts
 `enrichParcelLandRows` 입력 — 소유/출처 타입을 허용 값만으로 좁힘

수정 | src/service/sourceInstallZipService.ts
 ZIP entry 옵션 — store/zlib을 타입 단언으로 전달

수정 | src/service/sourceUploadRemote.ts
 `RemoteUploadStageId` — npmInstall 단계 추가

수정 | src/app/(pages)/map/page.tsx · layout.tsx
 Suspense — useSearchParams 사용으로 인한 /map prerender 오류 방지

## 테이블 수정

해당 없음

## 테스트

1. `npm run build`가 끝까지 성공해야 한다.
2. `/map` 페이지가 빌드 목록에 포함되어 오류 없이 생성되어야 한다.
3. 파일변환 PDF→JPG 탭이 타입 오류 없이 빌드되어야 한다.

## 프롬프트

해당 없음
해당 없음
