# 결과보고서

2026-09-11 | dev.strcat | —

## 목적

기준점 등록 화면에서 속성 제목이 입력칸과 높이가 맞게 보이게 하고, 지도에 점을 찍으면 주소가 같이 들어가게 한다.

## 요약

등록 속성표 왼쪽 제목은 세로 가운데로 맞춘다. 지도를 클릭해 위치를 정하면 도로명 또는 지번 주소가 주소 칸에 채워진다. 번호는 형식이 제각각이라 직접 넣고, 비우면 등록되지 않는다. 메인 지도보기에는 검정 깔개 없이 남색만 깐다. 오른쪽 공지·자료실 높이는 왼쪽 카드와 맞춘다. 더보기 즐겨찾기는 눌러야 해당 기능이 열린다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/gcp/GcpDetailPanel.tsx
 `CREATE_LABEL` — 등록 속성표 왼쪽 제목을 입력칸과 세로 가운데로 맞춤
 `fillAddress` — 지도에서 위치를 찍으면 좌표로 주소를 찾아 주소 칸에 넣음
 `canCreate` — 번호가 비어 있으면 등록 버튼을 끄고, 공백만 있어도 등록되지 않음
 `GCP번호` — 필수 표시. 비어 있으면 등록 버튼을 끔

수정 | src/app/(pages)/(index)/map-view-link.tsx
 `MapViewLink` — 검정 깔개 없이 남색만 깔고, 영상 크기는 그대로 맞춤

수정 | src/app/(pages)/(index)/notice-library-preview.tsx
 `NoticeLibraryPreview` — 왼쪽 카드와 같은 높이로 맞춤

수정 | src/app/(pages)/(index)/page.tsx
 상단 구역 — 왼쪽 카드와 오른쪽 공지·자료실 높이를 같게 함

수정 | src/app/(pages)/map/_mapContents/prototypes/UserAccountMoreTab.tsx
 `FavoriteStrip` — 살짝 누를 때는 기능을 열고, 가로로 밀 때만 넘기게 함. 예전에는 누르기만 해도 열리지 않았음

## 테이블 수정

해당 없음

## 테스트

1. 기준점 등록 화면에서 상세 속성 왼쪽 제목이 입력칸과 세로 가운데로 맞아야 한다.
2. 등록 화면에서 지도를 클릭하면 주소 칸에 그 위치 주소가 들어가야 한다.
3. 주소를 찾지 못하면 주소 칸이 비어 있어야 한다.
4. 주소는 직접 고쳐 쓸 수 있어야 한다.
5. 이미 있는 점 상세에서는 지도를 찍어 주소가 바뀌지 않아야 한다.
6. 등록 화면에서 번호가 비어 있으면 등록 버튼이 꺼져 있어야 한다.
7. 번호에 공백만 넣어도 등록되지 않아야 한다.
8. 번호는 직접 넣는 칸이어야 하고 자동으로 채워지지 않아야 한다.
9. 메인 지도보기 카드에 검정 깔개는 없고 남색만 깔려 있어야 한다.
10. 메인 오른쪽 공지사항·자료실 높이가 왼쪽 카드와 같아야 한다.
11. 더보기 즐겨찾기를 누르면 해당 기능이 열려야 한다.
12. 즐겨찾기가 한 줄을 넘으면 가로로 밀어서 넘길 수 있어야 한다.

## 프롬프트

해당 없음
docs/plan/20260910_1508_지상기준점.md

---

2026-09-11 | JungWooojin | QGIS연결 패널 UI 개선

## 목적

QGIS연결 관리 화면을 다른 지도 목록 패널과 비슷한 톤으로 다듬어 쓰기 쉽게 한다.

## 요약

헤더·검색·선택 강조·복사/저장 안내를 정리했다. 사용자 검색을 추가하고, 키·주소 복사와 저장 완료를 짧게 보여 준다. 하단 접속 주소는 복사 버튼으로 빼 두었다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/qgisLayerControl/QgisLayerControlPanel.tsx
 `QgisLayerControlPanel` — 배경·테두리·선택행을 공통 토큰으로 맞춤. 헤더에 건수·닫기 아이콘
 `userFilter` — 왼쪽 사용자·키 검색
 `flashCopy` / `savedHint` — 키·주소 복사·저장 완료 짧은 안내
 하단 WFS 주소 — 주소 복사 버튼 + 주소 문구 정리

## 테이블 수정

해당 없음

## 테스트

1. QGIS연결을 열면 헤더에 사용자 수 요약이 보여야 한다.
2. 왼쪽에서 아이디·이름으로 사용자를 검색할 수 있어야 한다.
3. 사용자를 고르면 행이 하늘색으로 강조되어야 한다.
4. 키·주소 복사 시 상단에 복사 안내가 잠깐 보여야 한다.
5. 권한 저장 후 저장됨 표시가 잠깐 보여야 한다.
6. 읽기·수정 스위치·키 발급·삭제는 이전과 같이 동작해야 한다.

## 프롬프트

docs/brief/20260911_1342_QGIS레이어권한.md
docs/plan/20260911_1342_QGIS레이어권한.md

---

2026-09-11 | JungWooojin | QGIS연결 패널 표준 UI 재맞춤

## 목적

앞에서 손본 QGIS연결 화면이 다른 목록 패널과 안 맞고 선택 색이 잘리던 문제를 고친다.

## 요약

보상·도로 목록과 같은 표준 패널·목록 클래스로 다시 맞췄다. 선택 표시는 왼쪽 강조선만 쓰고 링을 빼 잘림을 없앴다. 검색·푸터·테이블 헤더도 공통 스타일을 쓴다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/qgisLayerControl/QgisLayerControlPanel.tsx
 `QgisLayerControlPanel` — standard-panel / standard-list / standard-table 클래스로 재구성
 선택 행 — standard-list-row-selected (왼쪽 보더). 스크롤 끝에서 색 잘리던 ring 제거

## 테이블 수정

해당 없음

## 테스트

1. QGIS연결을 열면 다른 목록 패널과 비슷한 헤더·검색·푸터 톤이어야 한다.
2. 사용자를 선택해도 행 색이 좌우로 잘리지 않아야 한다.
3. 목록을 스크롤해도 헤더가 위에 남아야 한다.
4. 키 발급·권한 저장·주소 복사는 이전과 같이 동작해야 한다.

## 프롬프트

docs/brief/20260911_1342_QGIS레이어권한.md
docs/plan/20260911_1342_QGIS레이어권한.md

---

2026-09-11 | JungWooojin | 메뉴명 변경

## 목적

사이드바·패널에 보이던 «QGIS연결» 이름을 짧게 «레이어권한»으로 바꾼다.

## 요약

메뉴 표시명과 패널 제목만 변경했다. 내부 식별자는 그대로다.

## 파일수정

수정 | src/config/serviceList.config
 `ser_kor` — «QGIS연결» → «레이어권한»

수정 | src/app/(pages)/map/_mapContents/qgisLayerControl/QgisLayerControlPanel.tsx
 패널 제목 — «레이어권한»로 통일

## 테이블 수정

해당 없음

## 테스트

1. 사이드바에 «레이어권한»이 잘리지 않고 보여야 한다.
2. 메뉴를 열면 패널 제목도 «레이어권한»이어야 한다.
3. «QGIS연결»·«레이어작업관리» 문구는 사이드바·패널에 남아 있지 않아야 한다.

## 프롬프트

해당 없음
docs/plan/20260911_1342_QGIS레이어권한.md
