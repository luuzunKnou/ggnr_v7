# 결과보고서

2026-07-13 | 200BingSu | 버전관리 node --watch 재시작 모드

## 목적

최신 소스 적용 후 `node --watch`로 `scripts/run.ts`(npm run dev와 동일 진입)를 새 창에서 다시 띄울 수 있게 한다.

## 요약

재시작 방식에 «node --watch 재시작» 라디오를 추가했다. 선택 시 구동 프로젝트·타입으로 `node --watch --import tsx scripts/run.ts dev …`를 조합해, 명령 실행과 같이 포트 해제 후 새 창에서 기동한다.

## 파일수정

수정 | src/service/sourceVersionService.ts
 `RestartMode` — `nodeWatch` 추가
 `resolveRestartCommand` — nodeWatch 시 `node --watch --watch-preserve-output --import tsx scripts/run.ts …` 조합
 `scheduleRestart` — command와 동일하게 새 창·포트 대기 후 실행

수정 | src/app/(pages)/dev/_components/VersionManagerContent.tsx
 라디오 — «node --watch 재시작» (재시작 안 함 앞)

수정 | src/lib/sourceVersionClientRelay.ts
 `RestartMode` — nodeWatch 포함, 사전 검증 동일

수정 | src/app/api/source/version/relay/init/route.ts
 `toRestartMode` — nodeWatch 허용

수정 | src/app/api/source/version/update-latest/route.ts
 `toRestartMode` — nodeWatch 허용

## 테이블 수정

해당 없음

## 테스트

1. 버전관리에 «node --watch 재시작» 라디오가 «재시작 안 함» 앞에 보여야 한다.
2. 해당 방식으로 적용하면 새 창에서 node --watch로 run.ts가 떠야 한다.
3. 재기동 후 앱 포트가 이전과 같아야 한다.
4. 이후 소스 파일 변경 시 watch가 프로세스를 다시 띄우는지 확인할 수 있어야 한다.
5. 명령 실행·process.exit·재시작 안 함은 이전과 같아야 한다.

## 프롬프트

—
—
