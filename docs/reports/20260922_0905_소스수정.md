# 결과보고서

2026-09-22 | dev.strcat | 레이어 geom·스타일 타입 맞춤

## 목적

지오메트리 유형과 어긋난 면·점 스타일을 찾아 실제 geom에 맞게 고친다.

## 요약

프로젝트 DB를 기준으로 스타일과 도형을 대조했다. 석축은 선인데 면 스타일이었고, 정수장은 면인데 점(아이콘) 스타일이었다. 둘 다 맞췄고, 도로대장 테이블 메타의 도형 유형도 DB와 맞게 고쳤다.

## 파일수정

수정 | geoserver_modules/data_dir/styles/f9047226.css
 석축 스타일 — 면 fill → 선 이중 stroke

수정 | geoserver_modules/data_dir/styles/f9047226.sld
 `LineSymbolizer` — CSS와 동일 라인 심볼

수정 | geoserver_modules/data_dir/styles/wtl_puri_as.css
 정수장 스타일 — 점 아이콘 → 면 fill·외곽선

수정 | geoserver_modules/data_dir/styles/wtl_puri_as.sld
 `PolygonSymbolizer` — CSS와 동일 면 심볼

수정 | src/config/defineLayer/tables.json
 도로대장 등 17개 테이블 도형 유형 — DB 실제 점·선·면에 맞게 수정

## 테이블 수정

해당 없음

## 테스트

1. 도로대장 배수시설에서 석축을 켜면 선으로 보여야 한다.
2. 옹벽·절개·성토도 선으로 보여야 한다.
3. 정수장 레이어는 면으로 채워져 보여야 한다.
4. 표지·신호등·가로수 등 점 시설은 기존처럼 점으로 보여야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 기본계획 사유지 프로토타입

## 목적

기본계획 상세에서 시작선·종료선 사이와 겹치는 소유 필지를 목록으로 볼 수 있는지 확인한다.

## 요약

기본계획 상세에 사유지 버튼을 넣었다. 선택한 하천 경계에 직각으로 두 선을 그으면, 그 사이와 하천이 겹치는 필지의 소유구분 건수·주소·면적이 접힌 목록으로 나온다. 행을 누르면 아래에 행망 소유자 내용이 붙는다.

## 파일수정

추가 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanPrivateLandProto.tsx
 `RiverBasicPlanPrivateLandProto` — 사유지 버튼, 직각 두 선, 접힘 목록, 행 클릭 시 소유자

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanDetailPanel.tsx
 상세 버튼 아래에 사유지 프로토타입 연결

수정 | src/service/riverBasicPlanService.ts
 `getRiverBasicPlanAreaGeom3857` — 선택 기본계획 하천 면
 `listRiverBasicPlanPrivateLand` — 선 사이와 하천이 겹치는 지적·소유구분

## 테이블 수정

해당 없음

## 테스트

1. 기본계획을 고른 뒤 사유지를 누르면 시작선을 긋는 안내가 나와야 한다.
2. 선은 하천 경계에 직각으로만 그어져야 한다.
3. 종료선까지 그으면 접힌 상태로 소유구분별 건수가 나와야 한다.
4. 펼치면 주소·소유구분·면적 목록이 나와야 한다.
5. 행을 누르면 아래에 소유자 내용이 나와야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 기본계획 사유지 목록 위치

## 목적

사유지 버튼을 다른 버튼과 같게 두고, 목록은 색인도 아래에 소유구분별로 보이게 한다.

## 요약

사유지 버튼을 보고서·색인도와 같은 줄에 맞췄다. 선은 자유롭게 긋고, 결과는 색인도 아래에 소유구분 한글 이름마다 접힌 목록으로 둔다. 주소를 누르면 그 목록 아래에 소유 상세가 나온다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanPrivateLandProto.tsx
 `RiverBasicPlanPrivateLandButton` — 기존 버튼과 같은 줄·모양
 `RiverBasicPlanPrivateLandSection` — 색인도 아래, 소유구분별 접힘 목록과 주소 클릭 상세
 선 긋기 — 직각 고정 없이 자유 직선

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanDetailPanel.tsx
 사유지 버튼은 상단 버튼 줄, 목록은 색인도 아래

## 테이블 수정

해당 없음

## 테스트

1. 사유지 버튼이 보고서·색인도와 같은 줄·같은 크기로 보여야 한다.
2. 선은 원하는 방향으로 자유롭게 그어져야 한다.
3. 결과를 색인도 아래에서 소유구분 한글 이름별로 나눠 봐야 한다.
4. 구분을 열면 주소·면적만 나오고, 주소를 누르면 그 아래에 소유 상세가 나와야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지·색인도 접기

## 목적

상세가 아래로만 길어지지 않게 색인도를 접고, 사유지는 색인도와 같이 구분을 눌러 안쪽 화면으로 보게 한다.

## 요약

색인도에 접기를 넣었다. 사유지는 소유구분 요약만 두고, 구분을 누르면 색인도처럼 상세 안쪽에서 목록과 소유 표를 본다. 소유 정보는 기존 상세 속성표 형태로 맞췄다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanDetailPanel.tsx
 색인도 — 접기·펼치기, 건수 표시
 사유지 구분 선택 시 — 스크롤 영역을 내부 상세로 전환

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanPrivateLandProto.tsx
 `RiverBasicPlanPrivateLandSection` — 소유구분 요약 그리드만
 `RiverBasicPlanPrivateLandCategoryPanel` — 목록·소유 정보(속성표 UI)

## 테이블 수정

해당 없음

## 테스트

1. 색인도 제목을 누르면 목록이 접혀야 한다.
2. 사유지에서 소유구분을 누르면 색인도와 같이 안쪽 화면으로 바뀌어야 한다.
3. 안쪽에서 뒤로 가면 요약 목록으로 돌아와야 한다.
4. 주소를 고르면 소유 정보가 기존 속성표처럼 보여야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지 범위 하천 경계

## 목적

두 선 사이 범위를 직선 사각형이 아니라 하천 경계를 따라가게 한다.

## 요약

시작·종료선으로 만든 띠와 하천 면이 겹친 구간만 지도에 그린다. 필지 조회도 그 구간을 쓴다.

## 파일수정

수정 | src/service/riverBasicPlanService.ts
 `listRiverBasicPlanPrivateLand` — 하천과 겹친 구간 도형도 함께 반환

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanPrivateLandProto.tsx
 범위 표시 — 직선 띠 대신 하천을 따라간 구간

## 테이블 수정

해당 없음

## 테스트

1. 시작·종료선을 그으면 파란 범위가 하천 밖으로 삐져나오지 않아야 한다.
2. 범위 가장자리가 하천 경계와 같아야 한다.
3. 필지 목록은 그 하천 구간과 겹친 것만 나와야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지 두 선 사이 하천 분할

## 목적

시작·종료선 사이 하천을 직선 띠가 아니라 하천을 잘라 사이 구간으로 잡는다.

## 요약

두 선으로 하천 면을 자른 뒤, 양쪽 선에 모두 닿는 조각만 범위로 쓴다. 굽은 구간도 하천 경계를 따라간다.

## 파일수정

수정 | src/service/riverBasicPlanService.ts
 `listRiverBasicPlanPrivateLand` — 시작·종료선으로 하천 분할 후 사이 구간 선택

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanPrivateLandProto.tsx
 조회 — 직선 띠 대신 두 선 WKT 전달

## 테이블 수정

해당 없음

## 테스트

1. 굽은 하천에서 두 선을 그으면 범위가 하천 밖으로 직선으로 잘리지 않아야 한다.
2. 아래쪽 빈 구간 없이 하천 경계를 따라가야 한다.
3. 선이 하천을 가로지르지 않으면 안내가 나와야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지 제목 패딩

## 목적

사유지 제목 여백을 위 색인도와 같게 맞춘다.

## 요약

사유지 접기 제목을 색인도와 같은 글자·여백으로 바꿨다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanPrivateLandProto.tsx
 사유지 제목 — 색인도와 동일 접기 헤더 스타일

## 테이블 수정

해당 없음

## 테스트

1. 사유지·색인도 제목의 왼쪽 여백이 같아야 한다.
2. 접기 화살표·총 n건 위치도 색인도와 맞아야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지 카드·필지 지도 표시

## 목적

사유지 요약 카드와 내부 목록·소유정보를 색인도·기본계획과 같은 여백·회색으로 맞추고, 필지 선택 시 지도에 겹친 필지를 보이게 한다.

## 요약

요약 카드에 건수를 「n건」으로 표기하고, 소유구분 목록·소유정보 표를 기본계획과 같은 회색·패딩으로 맞췄다. 목록에서 필지를 고르면 지도에 해당 필지가 강조되고 면적은 ㎡로 표시된다.

## 파일수정

수정 | src/service/riverBasicPlanService.ts
 `listRiverBasicPlanPrivateLand` — 필지 응답에 도형·범위를 포함해 지도 표시에 사용

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanPrivateLandProto.tsx
 `SUMMARY_CHIP_CLASS` — 색인도와 동일 카드 패딩, 건수에 「건」 표기
 `syncParcelFeatures` — 소유구분·필지 선택 시 지도에 필지 표시·이동
 `RiverBasicPlanPrivateLandCategoryPanel` — 목록·소유정보 회색표, 면적 ㎡, 리스트와 동일 여백

## 테이블 수정

해당 없음

## 테스트

1. 사유지 카드와 색인도 카드의 안쪽 여백이 같아야 한다.
2. 사유지 카드 오른쪽 숫자가 「22건」처럼 나와야 한다.
3. 소유구분을 열면 목록 헤더가 기본계획 연도표와 같은 회색이어야 한다.
4. 목록 행을 누르면 지도에 해당 필지가 보여야 한다.
5. 면적 칸에 ㎡가 붙어야 한다.
6. 소유정보 표도 같은 회색·여백이어야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지 목록 토글·지도연동

## 목적

사유지 상세 목록에서 선택 해제·높이·헤더 고정·지도 클릭 연동을 맞춘다.

## 요약

같은 행을 다시 누르면 선택이 해제되고, 목록은 더 높게 보이며 헤더는 스크롤되지 않는다. 상세가 열린 동안 지도에서 필지를 누르면 목록이 같이 선택된다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanPrivateLandProto.tsx
 `pick` — 같은 필지 재클릭 시 선택 해제
 지도 `singleclick` — 사유지 상세 활성 시 필지 클릭으로 목록 선택
 `RiverBasicPlanPrivateLandCategoryPanel` — 목록 높이 확대, 헤더 고정, 선택 행 스크롤

## 테이블 수정

해당 없음

## 테스트

1. 목록 행을 다시 누르면 선택이 해제되고 소유정보가 사라져야 한다.
2. 목록 영역이 이전보다 더 높게 보여야 한다.
3. 목록을 스크롤해도 No·주소·면적 헤더는 위에 남아야 한다.
4. 사유지 상세가 열린 상태에서 지도 필지를 누르면 해당 행이 선택되어야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지 두 선 안쪽 구간

## 목적

시작·종료선 사이 하천만 잡히도록 가로 절단 시 잘못된 끝 구간 선택을 고친다.

## 요약

연장 절단선에 양쪽이 닿는 조각이 아니라, 한 선 기준으로 다른 선 쪽(안쪽)에 있는 조각만 남긴다. 가로로 그어도 두 선 사이 하천만 선택된다.

## 파일수정

수정 | src/service/riverBasicPlanService.ts
 `listRiverBasicPlanPrivateLand` — 두 선 안쪽 반평면 기준으로 하천 구간 선택

## 테이블 수정

해당 없음

## 테스트

1. 세로로 두 선을 그으면 그 사이 하천만 파랗게 잡혀야 한다.
2. 가로로 두 선을 그어도 두 선 밖 끝 구간이 잡히지 않아야 한다.
3. 굽은 하천에서도 두 선 사이 구간만 남아야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지 가로선 구간 재수정

## 목적

왼쪽에 두 선을 그었는데 오른쪽 하천이 활성화되던 문제를 고친다.

## 요약

반평면·연장선 교차 대신, 사용자가 그린 짧은 시작·종료선에 모두 닿는 조각만 두 선 사이로 본다. 가로로 그어도 선 사이만 잡혀야 한다.

## 파일수정

수정 | src/service/riverBasicPlanService.ts
 `listRiverBasicPlanPrivateLand` — 짧은 시작·종료선에 모두 근접한 조각만 선택

## 테이블 수정

해당 없음

## 테스트

1. 왼쪽 근처에 가로 두 선을 그으면 그 사이만 파랗게 잡혀야 한다.
2. 선 오른쪽(하류) 긴 구간이 활성화되지 않아야 한다.
3. 세로 두 선도 사이 구간만 잡혀야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 구글자동번역 차단

## 목적

크롬 영어 자동번역이 페이지 DOM을 바꿔 우클릭 등에서 클라이언트 예외가 나는 것을 막는다.

## 요약

루트 레이아웃에 번역 금지 메타·속성을 넣고 언어를 한국어로 맞췄다. Next는 index.html 대신 이 레이아웃이 적용된다.

## 파일수정

수정 | src/app/layout.tsx
 루트 html — lang 한국어, 번역 금지·notranslate, google notranslate 메타

## 테이블 수정

해당 없음

## 테스트

1. 크롬 자동번역이 켜져 있어도 사이트에 번역 바·자동번역이 걸리지 않아야 한다.
2. 우클릭 필지정보 시 «클라이언트 측 예외»가 나지 않아야 한다.

## 프롬프트

—
—

---

2026-09-22 | dev.strcat | 사유지 버튼 숨김

## 목적

하천기본계획 상세의 사유지 버튼을 당분간 화면에서 뺀다.

## 요약

사유지 프로토타입 버튼만 숨겼다. 코드는 남겨 두었고 다시 켤 수 있다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/river/riverBasicPlan/RiverBasicPlanDetailPanel.tsx
 사유지 버튼 — 상세 버튼 줄에서 제거(임시 숨김)

## 테이블 수정

해당 없음

## 테스트

1. 기본계획 상세에 사유지 버튼이 보이지 않아야 한다.

## 프롬프트

—
—
