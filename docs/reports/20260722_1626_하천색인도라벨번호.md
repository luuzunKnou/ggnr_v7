# 결과보고서

2026-07-22 | gyujin | 하천색인도 라벨을 코드 끝 번호로 표시

## 목적

하천색인도 지도 라벨이 0만 나오던 문제를, 관리코드 끝 번호가 보이도록 고친다.

## 요약

색인도 스타일 라벨을 도면명 구간 자르기에서 관리코드 끝 세 자리 숫자 추출로 바꿨다. GeoServer 스타일도 재등록해 바로 반영했다.

## 파일수정

수정 | geoserver_modules/data_dir/styles/river_d_index.css
 라벨 식 — `draw_name` 11~14자 자르기 → `cons_code` 끝 3자리 `parseInt` (예: INDEX_013 → 13)

수정 | geoserver_modules/data_dir/styles/river_d_index.sld
 라벨 식 — CSS와 동일하게 `cons_code` 끝 번호 표시

## 테이블 수정

해당 없음

## 테스트

1. 하천색인도 레이어를 켜고 확대하면 폴리곤에 0이 아닌 번호가 보여야 한다.
2. 관리코드가 MB_009_INDEX_013 인 도형은 13이 보여야 한다.
3. 관리코드가 MB_009_INDEX_001 인 도형은 1이 보여야 한다.
4. 축소(라벨 축척 밖)에서는 경계만 보이고 라벨은 없어야 한다.

## 프롬프트

docs/brief/ — 해당 없음
docs/plan/20260722_1626_하천색인도라벨번호.md
