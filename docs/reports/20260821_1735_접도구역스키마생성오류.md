# 결과보고서

2026-08-21 | JungWooojin | 접도구역 건축물 스키마 생성 오류

## 목적

목록을 열 때 나온 스키마 생성 실패 안내가 다시 나오지 않게 한다. 저장용 테이블은 이미 있다.

## 요약

목록·저장마다 스키마를 다시 만들려다 실패한 메시지였다. 스키마가 있으면 만들지 않고, 본문 테이블이 있으면 생성 절차를 건너뛴다.

## 파일수정

수정 | src/service/ensureLayerAppTables.ts
 `ensureSchemaLayer` — 스키마가 이미 있으면 생성 쿼리를 실행하지 않음. 생성 실패여도 스키마가 있으면 넘어감

수정 | src/service/roadFrontageBuildingService.ts
 `ensureTables` — 본문 테이블이 있으면 스키마·테이블 생성을 호출하지 않음

## 테이블 수정

해당 없음

## 테스트

1. 접도구역 건축물 목록이 오류 없이 열려야 한다.
2. 추가 후 저장이 되어야 한다.
3. 스키마 생성 실패 안내가 다시 나와서는 안 된다.

## 프롬프트

docs/brief/ — 해당 없음
docs/plan/20260821_1622_접도구역건축물저장연동.md
