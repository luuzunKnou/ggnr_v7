# 결과보고서

2026-08-14 | gyujin | master → minami 병합 충돌 해결

## 목적

`minami` 브랜치에 `master`를 병합하면서 생긴 충돌 6건을 양쪽 변경이 함께 동작하도록 맞춘다.

## 요약

지도 서비스 레이어 동기화·점용 스타일·첨부 미리보기·도로망도 목록·보상편입 필지 모달에서 충돌이 났다. 점사용료 하단 정렬·CQL 생략·목록 로딩·필지 삭제 등 양쪽 기능을 유지했다.

## 파일수정

수정 | src/app/(pages)/map/_mapComponents/OpenLayersMap.tsx
 `useServiceLayerSync` — 레이어별 추가 조회 조건과 점사용료 하단 정렬 인자를 함께 넘기도록 맞춤

수정 | src/app/(pages)/map/_mapComponents/layerFactory/serviceLayerFactory.ts
 `syncKey` — 조회 조건이 없을 때 필터를 빼고, 하단 레이어 변경도 동기화 키에 포함

수정 | src/app/(pages)/map/_mapComponents/standard/ServiceFileImagePreview.tsx
 미리보기 이미지 — 화면 중앙 정렬과 큰 미리보기 크기를 함께 적용. 중복 className 제거

수정 | src/app/(pages)/map/_mapContents/road/roadNetwork/RoadNetworkListPanel.tsx
 헤더 추가 버튼 — 공통 추가 버튼으로 맞추고, 신규 행이 있으면 비활성화
 목록 영역 — 불러오는 중·오류 안내를 유지하고 스크롤 표시는 다른 목록과 같게

수정 | src/app/(pages)/map/_mapContents/road/roadReward/RoadRewardParcelModal.tsx
 하단 버튼 — 조회만일 때도 닫기를 두고, 수정 모드에서는 저장·삭제를 함께 표시

수정 | src/lib/occupationLayerStyle.ts
 `occupationFillRgba` — 투명도를 생략하면 레이어 기본값을 쓰도록 맞춤

## 테이블 수정

해당 없음

## 테스트

1. 지도에서 점용대장·점사용료를 켜면 점사용료가 아래에 깔리고 본표가 위에 보여야 한다.
2. 조회 조건이 없는 레이어는 지도가 정상 표시되어야 한다.
3. 첨부파일 미리보기에서 이미지가 화면 중앙에 크게 보여야 한다.
4. 도로망도 목록에서 엑셀·추가 버튼이 보이고, 신규 행이 있으면 추가가 비활성화되어야 한다.
5. 도로망도 목록을 불러오는 중·실패 시 안내 문구가 나와야 한다.
6. 보상편입 필지 조회 모달에는 닫기만 보이고, 수정 모달에는 저장·삭제·닫기가 보여야 한다.

## 프롬프트

해당 없음
해당 없음
