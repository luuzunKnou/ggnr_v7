# 결과보고서

2026-07-14 | 200BingSu | 설치ZIP진행상태선등록

## 목적

설치 ZIP 다운로드 시 진행 조회를 등록보다 먼저 호출해 첫 폴링이 404가 나던 순서를 고친다.

## 요약

업로드와 같이 진행 ID를 서버에 먼저 등록한 뒤 폴링을 시작하고, 그다음 ZIP 생성을 요청한다. 생성 API는 이미 등록된 진행을 덮어쓰지 않는다.

## 파일수정

수정 | src/app/api/source/version/install-zip/progress/route.ts
 `POST` — 진행 ID 선등록 후 대기 상태 설정

수정 | src/app/(pages)/dev/_components/InstallZipDownloadPanel.tsx
 `downloadInstallZip` — 등록 → 폴링 → ZIP 생성 순서로 변경

수정 | src/app/api/source/version/install-zip/build/route.ts
 `POST` — 미등록일 때만 진행 초기화

## 테이블 수정

해당 없음

## 테스트

1. 설치 ZIP 다운로드 시작 직후 진행 조회가 404 없이 응답해야 한다.
2. 스캔·ZIP 생성 중 진행 단계가 갱신되어야 한다.
3. 다운로드까지 정상 완료되어야 한다.

## 프롬프트

—
—
