# 결과보고서

2026-09-16 | JungWooojin | —

## 목적

지상기준점에서 점검 이력 조회·저장 방향을 접고, 목록·상세·점검 추가(상태 갱신)만 남긴다.

## 요약

상세의 «점검 이력» 목록과 이력 상세 창·시드 이력 데이터를 제거했다. 점검 추가는 최근 점검일·상태·사진만 갱신하고 이력을 쌓지 않는다.

## 파일수정

삭제 | src/app/(pages)/map/_mapContents/gcp/GcpInspectHistoryDialog.tsx
 점검 이력 상세 대화창 제거

수정 | src/app/(pages)/map/_mapContents/gcp/GcpDetailPanel.tsx
 점검 이력 섹션·이력 열기 상태 제거. 목록·사진·점검 추가·편집 유지

수정 | src/app/(pages)/map/_mapContents/gcp/gcpDummyData.ts
 `GCP_SEED_INSPECTIONS` 시드 이력 제거

수정 | src/app/(pages)/map/_mapContents/gcp/gcpProtoStore.ts
 이력 배열·조회 제거. `applyGcpInspection`은 점 상태만 갱신

수정 | src/app/(pages)/map/_mapContents/gcp/useGcpProtoState.ts
 `inspections` 구독 제거

수정 | src/app/(pages)/map/map-layout-client.tsx
 상세 패널 이력 오버레이용 위치 props 전달 제거

## 테이블 수정

해당 없음

## 테스트

1. 지상기준점 상세에 «점검 이력» 목록이 없어야 한다.
2. 목록·지도·상세 속성·사진이 이전과 같이 보여야 한다.
3. 점검 추가 저장 후 최근 점검일·상태가 바뀌어야 한다.
4. 기준점 추가·편집이 동작해야 한다.

## 프롬프트

—
—

---

2026-09-16 | JungWooojin | —

## 목적

지상기준점에서 상태·점검 개념까지 제거하고, 기준점 위치·속성 관리만 남긴다.

## 요약

상태·최근 점검일·점검 추가·상태 필터·상태별 지도색을 없앴다. 목록은 번호·주소·설치일, 상세는 좌표·위치설명·주소·사진 중심으로 둔다.

## 파일수정

삭제 | src/app/(pages)/map/_mapContents/gcp/GcpInspectDialog.tsx
 점검 추가 화면 제거

수정 | src/app/(pages)/map/_mapContents/gcp/gcpConfig.ts
 상태·점검 주기 상수 제거. 지도색 단일화

수정 | src/app/(pages)/map/_mapContents/gcp/gcpDummyData.ts
 점 모델에서 상태·최근 점검일·점검 타입 제거

수정 | src/app/(pages)/map/_mapContents/gcp/gcpProtoStore.ts
 점검 적용 API 제거. 속성 갱신은 위치설명·주소만

수정 | src/app/(pages)/map/_mapContents/gcp/GcpListPanel.tsx
 상태 필터·점검 추가·상태/최근점검 열 제거

수정 | src/app/(pages)/map/_mapContents/gcp/GcpDetailPanel.tsx
 상태·최근 점검일·점검 추가 제거

수정 | src/app/(pages)/map/_mapContents/gcp/useGcpMapLayer.ts
 지도 점색 단일색

수정 | src/app/(pages)/map/map-layout-client.tsx
 점검 패널 배선 제거

## 테이블 수정

해당 없음

## 테스트

1. 목록에 상태·최근 점검·점검 추가가 없어야 한다.
2. 상세에 상태·최근 점검일·점검 추가가 없어야 한다.
3. 지도 기준점이 한 가지 색으로 보여야 한다.
4. 기준점 추가·상세 편집·검색이 동작해야 한다.

## 프롬프트

—
—

---

2026-09-16 | JungWooojin | —

## 목적

GCP 목록이 실패하던 원인을 고쳐 업로드 자료가 보이게 한다.

## 요약

업로드 도형이 다중점이라 좌표 추출이 실패했다. 다중점에서도 좌표를 읽도록 바꿨다.

## 파일수정

수정 | src/service/gcpService.ts
 목록 좌표 추출 — 다중점 대응. 신규 등록도 동일 도형 형식

수정 | src/app/(pages)/map/_mapContents/gcp/useGcpData.ts
 실패 시 서버 오류 문구 표시

## 테이블 수정

해당 없음

## 테스트

1. GCP 메뉴를 열면 업로드 점(약 85건)이 목록에 보여야 한다.
2. 지도에도 점이 표시되어야 한다.

## 프롬프트

—
—

---

2026-09-16 | JungWooojin | —

## 목적

GCP 상세에서 위치 사진을 1장만 첨부·교체·삭제할 수 있게 한다.

## 요약

상세에 첨부 사진 영역을 넣었다. 이미지 1장만 두고, 새로 올리면 기존 사진은 교체된다. 저장 경로는 기존 첨부 파일 방식을 쓴다.

## 파일수정

수정 | src/lib/serviceFileDataSerEng.ts
 GCP 첨부용 서비스 키 추가

추가 | src/app/(pages)/map/_mapContents/gcp/GcpPhotoSection.tsx
 사진 1장 미리보기·첨부·교체·삭제

수정 | src/app/(pages)/map/_mapContents/gcp/GcpDetailPanel.tsx
 상세에 첨부 사진 영역 연결

## 테이블 수정

해당 없음

## 테스트

1. GCP 상세에 첨부 사진 영역이 보여야 한다.
2. 사진을 올리면 미리보기가 나와야 한다.
3. 다른 사진을 올리면 기존 사진이 바뀌어야 한다.
4. 삭제를 누르면 사진이 사라져야 한다.
5. 이미지 외 파일은 첨부되지 않아야 한다.

## 프롬프트

—
—

---

2026-09-16 | JungWooojin | —

## 목적

메뉴·패널 이름을 GCP로 통일하고, 원본 자료에 없는 설치일 필드를 뺀다.

## 요약

사이드바·목록·상세 표시명을 GCP로 바꿨다. 시험용으로 넣었던 설치일(최초 설치일)은 제거했다.

## 파일수정

수정 | src/config/serviceList.config
 메뉴 한글명 — GCP

수정 | src/config/systemList.config
 관리 시스템 소개 태그 — GCP

수정 | src/app/(pages)/map/_mapContents/gcp/GcpListPanel.tsx
 패널 제목 GCP. 설치일 열 제거

수정 | src/app/(pages)/map/_mapContents/gcp/GcpDetailPanel.tsx
 상세 제목 GCP. 설치일 입력·표시 제거

수정 | src/app/(pages)/map/_mapContents/gcp/gcpDummyData.ts
 `installedAt` 필드 제거

수정 | src/app/(pages)/map/_mapContents/gcp/gcpProtoStore.ts
 등록 시 설치일 파라미터 제거

## 테이블 수정

해당 없음

## 테스트

1. 사이드바 메뉴가 «GCP»로 보여야 한다.
2. 목록·상세 제목이 GCP여야 한다.
3. 설치일·최초 설치일이 화면에 없어야 한다.
4. 번호·주소 검색·추가·편집이 동작해야 한다.

## 프롬프트

—
—
