# 결과보고서

2026-07-13 | 200BingSu | process.exit 감시 스크립트 재기동 가시성·로그

## 목적

감시 창에서 서버가 실제로 꺼졌는지·다시 켜졌는지 구분할 수 있게 한다.

## 요약

기동 회차·종료 코드·가동 시간·재시작 신호·대기·재기동 여부를 PowerShell에 색과 시각으로 남기고, 같은 내용을 런타임 로그 파일에도 이어 쓴다.

## 파일수정

수정 | scripts/restart-watch.ps1
 `Write-WatchLog` — 시각·등급으로 화면 출력, 런타임 로그 파일에 동일 기록
 기동 루프 — 최초/재시작 회차 배너, exitCode·가동시간, 신호 요약, 대기·재기동 판정 로그

## 테이블 수정

해당 없음

## 테스트

1. 감시 스크립트로 서버를 띄우면 최초 기동 배너와 로그 파일 경로가 보여야 한다.
2. process.exit 재시작으로 적용하면 종료 감지·신호·대기·재기동 회차 로그가 순서대로 나와야 한다.
3. 재기동 후 기동 회차가 이전보다 1 커야 한다.
4. 명령 실행·재시작 안 함으로 종료되면 감시가 다시 켜지 않는다는 안내가 나와야 한다.
5. 런타임 로그 파일에 화면과 같은 시각 로그가 남아야 한다.

## 프롬프트

—
—
