# 결과보고서

2026-08-21 | JungWooojin | 접도구역 건축물 스키마 오류 수정

## 목적

목록·저장 때 나오던 스키마 생성 실패 안내를 없앤다.

## 요약

목록을 열 때마다 스키마를 다시 만들려다 실패한 것이었다. 화면 요청에서는 그 생성을 하지 않는다. 서버 기동 때는 스키마가 있으면 만들지 않는다.

## 파일수정

수정 | src/service/roadFrontageBuildingService.ts
 `list` `get` `save` `remove` — 목록·저장·삭제 때 스키마 생성을 호출하지 않음. 이미 있는 본문을 읽고 저장만 함

수정 | src/service/ensureLayerAppTables.ts
 `ensureSchemaLayer` — 스키마가 있으면 생성하지 않음. 권한 부족·중복이어도 화면 오류로 올리지 않음

## 테이블 수정

해당 없음

## 테스트

1. 접도구역 건축물 목록이 오류 없이 열려야 한다.
2. 스키마 생성 실패 안내가 나와서는 안 된다.
3. 추가 후 저장이 되어야 한다.

## 프롬프트

docs/brief/ — 해당 없음
docs/plan/20260821_1622_접도구역건축물저장연동.md
