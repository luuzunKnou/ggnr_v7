# 결과보고서

2026-07-21 | 213am | 레이어 업로드 절차 확인 및 부족한 기능 개선

## 목적

업무지시·plan에 따라 dev 레이어 업로드 절차를 점검·개선했다. SHP·엑셀 업로드부터 DB·지도 서버·레이어 정의 반영, 정합성·이력·불일치 보완까지 단계별 상태를 확인할 수 있도록 정리했다.

## 요약

레이어 관리를 SHP·Excel 업로드, 이력, 설정, 오류수정을 한곳에서 다루는 화면으로 정리하고, SHP 폴더 업로드를 4단계 위저드로 재구성해 업무 순서에 맞춰 진행 상태를 확인할 수 있게 했다. 좌표계 선택(1/4), 구성요소 생성 여부 확인(2/4), 정합성 검증(3/4), 결과 확인(4/4)이 순서대로 이어지며, 미완료 종료 시에는 진행 중 이력과 미결 로그를 정리하도록 했다.

정합성 검증은 추가·변경·삭제·충돌 구분을 기준으로 비교하도록 정리했다. 필터 적용 시에는 화면에서 행만 숨기지 않고 서버에서 조건에 맞는 데이터를 다시 조회하도록 바꿔 목록, 전체 건수, 푸터 건수가 서로 일치하도록 맞췄다. 변경된 필드는 목록과 상세에서 모두 확인할 수 있게 했다. 위저드에서는 3단계 선택 내용을 보관했다가 4단계 완료 시 한 번에 반영하도록 바꿨다.

이력과 운영 확인 흐름도 보완했다. SHP 업데이트 이력 탭에서 배치·레이어별 결과를 조회할 수 있고, 목록 행 «이력»으로 테이블별 수정 이력을 바로 열 수 있게 했다. 목록·오류수정 탭에서는 DB, GeoServer, 레이어 정의 간 불일치를 찾아 자동 보정할 수 있도록 했으며, 스타일 범례 미리보기와 키필드·필드명·신규 테이블 정의 정비를 함께 반영했다.

GeoServer 연동은 워크스페이스 `ggnr` 기준으로 `layer`, `public_layer`용 데이터스토어 구조를 정리하고 관련 카탈로그와 스타일 참조를 맞췄다.

후속 과제로는 Excel 업로드·Excel 업데이트 이력의 단계 표시와 이력 연동 완성, 단계별 전용 재시도 버튼, 대용량 업로드 중단·재개 지원이 남아 있다.

## 파일수정

수정 | src/app/(pages)/dev/_components/layerManager/LayerManagerListTab.tsx
 `LayerManagerListTab` — 레이어 목록에서 테이블·레이어·스타일 상태를 함께 보고 다운로드·이력 조회로 이어지도록 정리
 `loadStyleInfo` / `rowHasInfraError` — GeoServer 조회 기준을 정리해 실제 오류만 남도록 보정
 `StylePreviewSwatch` — 행별 스타일 범례 미리보기 연결

추가 | src/app/(pages)/dev/_components/layerManager/StylePreviewSwatch.tsx
 `StylePreviewSwatch` — GeoServer 스타일 심볼·색상 미리보기

수정 | src/app/(pages)/dev/_components/layerManager/LayerManagerRowHistoryDialog.tsx
 `LayerManagerRowHistoryDialog` — 목록 행 «이력»에서 테이블별 수정·동기화 이력 조회

수정 | src/app/(pages)/dev/_components/layerManager/LayerManagerAutoSetupTab.tsx
 `LayerManagerAutoSetupTab` — DB·GeoServer·정의 불일치를 목록으로 보여 주고 행별·전체 자동 수정 지원

수정 | src/app/(pages)/dev/_components/layerManager/LayerManagerUploadDialogs.tsx
 `LayerManagerUploadDialogs` — 상단 SHP·Excel 업로드 모달 정리

수정 | src/app/(pages)/dev/_components/layerManager/LayerManagerUploadButtons.tsx
 `LayerManagerUploadButtons` — 업로드 버튼과 모달 진입 연결

수정 | src/app/(pages)/dev/_components/layerManager/layerManagerUploadBridge.ts
 `layerManagerUploadBridge` — 업로드 완료 후 목록·SHP 이력 새로고침 연결

수정 | src/app/(pages)/dev/_components/layerManager/layerSetupIssues.ts
 `LAYER_SETUP_ISSUE_LABELS` / `LAYER_SETUP_ISSUE_ORDER` — 오류 유형 라벨·표시 순서 정리

수정 | src/app/(pages)/dev/_components/LayerAttrManager.tsx
 `keyFieldOnly` — 키 관련 컬럼만 편집할 수 있는 축소 모드

수정 | src/app/(pages)/dev/_components/exl/ExlHistoryTab.tsx
 `ExlHistoryTab` — Excel 업데이트 이력 목록 표시. **미완** — SHP 이력 수준의 배치 상세, 정합성 연동, 업로드 완료 후 자동 전환은 후속 작업

재사용 | src/app/(pages)/dev/_components/exl/ExlWizardModal.tsx
 `ExlWizardModal` — 기존 Excel→DB·레이어 생성 마법사를 레이어 관리 «Excel 업로드» 진입에 연결. **미완** — SHP 위저드와 같은 단계 표시·상태·정합성 흐름은 후속 정비 필요

수정 | src/app/(pages)/dev/_components/shp/ShpWizardModal.tsx
 `TOTAL_STEPS` / `STEP_LABELS` — 4단계 절차를 업무 순서에 맞게 표시
 `statusRows` / `StatusCell` — Table·Layer·Style·Define 완료·누락 상태 표시
 `runComponentSetup` — 누락 구성요소만 일괄 생성하고 실패 행·단계를 기록
 `runConsistencyCheck` — 키 기준 SHP·DB 비교 후 스키마 불일치 시 다음 단계 진입 차단
 좌표계 탐지 — `.prj`, 폴더명, 경계 겹침 후보를 바탕으로 선택 흐름 연결
 `deferDbWrite` 연동 — 3단계 선택 내용을 보관했다가 4단계 완료 시 반영·이력 확정
 `abortWizardHistoryIfNeeded` — 미완료 종료 시 진행 중 이력·미결 로그 정리, 반영 후 종료 차단
 `LayerAttrManager` (`keyFieldOnly`) — 위저드 내 키필드 보정 모달 연결

수정 | src/app/(pages)/dev/_components/shp/ShpCrsCandidateModal.tsx
 `ShpCrsCandidateModal` — 좌표계 후보 목록과 미리보기 지도 선택

수정 | src/app/(pages)/dev/_components/shp/ShpCrsPreviewMap.tsx
 `ShpCrsPreviewMap` — 후보 좌표계별 SHP 위치 미리보기

수정 | src/app/(pages)/dev/_components/shp/SyncDetailModal.tsx
 `deferDbWrite` — 정합성 검증(3/4)에서 바로 저장하지 않고 업로드(4/4) 완료시 DB 반영
 `flatRows` / `applyFieldFilters` — 필터를 조건으로 서버 재조회
 구분·필드값·필드 체크 필터 UI — 신규·충돌·삭제·유지와 필드 조건 선택
 변경 필드 전체 표시 — 목록·상세에서 바뀐 컬럼 전체 표시
 푸터 건수 — 필터·페이지 이동 후에도 서버 기준 총 건수 유지

수정 | src/app/(pages)/dev/_components/shp/ShpUploadTab.tsx
 `StepBadge` / `FileLogEntry` — 후처리 로그의 단계별 상태·오류 표시
 `ShpWizardModal` — 폴더 업로드 위저드 진입 연결

수정 | src/app/(pages)/dev/_components/shp/ShpHistoryTab.tsx
 `ShpHistoryTab` — 배치·레이어별 상세 이력, 건수·결과·정합성 검증 조회

재사용 | src/app/(pages)/dev/_components/shp/ShpToDbWizardModal.tsx
 `ShpToDbWizardModal` — 기타기능 SHP→DB 화면에 동일한 구성요소·단계 표시·정합성 모달 패턴 재사용

수정 | src/service/shpUploadService.ts
 `detectShpCrsCandidates` / `getShpRawGeojson` — 좌표계 후보 탐지와 미리보기용 데이터 제공
 `processShpBatch` / `createTableFromShp` / `createGeoServerLayer*` / `createDefineTableAndFields*` — 구성요소 일괄 생성 처리
 `compareShpWithTable` / `compareShpFolderSchema` — 정합성·스키마 검사 처리
 `dropShpSyncTempTable` — 비교·조회 후 `_sync_` 임시 테이블 항상 삭제
 `commitSyncIntents` / `clearUnappliedSyncLogs` — 4단계 완료 시 반영 확정, 미반영 로그 정리
 `getSyncLogDetail` / `getSyncLogFieldNames` / `getSyncLogPendingKeys` — 정합성 내역 상세, 필드 필터, 서버 페이징 조회
 `enrichLightSyncLogRow` — 신규 행이 충돌로 보이던 오표시 문제 보정
 `buildSyncLogOpFilterClauses` / `getSyncLogs` — 구분·필드 필터를 목록·총 건수 조회에 반영

수정 | src/service/layerHistoryService.ts
 `createLayerDetailHistoryDraft` / `updateDetailCounts` / `updateLayerHistory` — 비교 단계 이력 초안과 건수 갱신
 `abortIncompleteLayerHistory` — 미완료 위저드 이력 취소 처리

수정 | src/service/devTestService.ts
 `setupGeoServerDb` — 워크스페이스 ggnr 기준 데이터스토어 생성·접속 정보 정리
 `createOrUpdateGeoServerLayer` — 정의·DB 스키마 기준으로 올바른 데이터스토어에 FeatureType 발행
 `scanLayerSetupIssues` / `fixLayerSetupIssues` — 불일치 스캔과 자동 수정 처리

재사용 | src/service/excelUploadService.ts / excelHistoryService
 Excel 업로드·이력 API — 기존 마법사·탭과 연동. **미완** — 레이어 관리 화면 기준 절차·이력 경험은 SHP 대비 후속 작업

추가 | src/lib/shpHistoryArchive.ts
 `shpHistoryArchive` — 동기화 이력별 SHP 스냅샷 저장·복원

수정 | src/lib/geoserverStyleUtils.ts
 `geoserverStyleUtils` — 스타일 생성·목록 조회 기준 정리

수정 | src/config/defineLayer/tables.json 및 fields/*.json
 키필드·필드명 정리와 신규·누락 테이블 정의 보강

수정 | geoserver_modules/data_dir/workspaces/ggnr/postgres_layer/datastore.xml
 `postgres_layer` 데이터스토어 — `layer` 스키마 PostGIS 접속 정보 정리

수정 | geoserver_modules/data_dir/workspaces/ggnr/postgres_public_layer/datastore.xml
 `postgres_public_layer` 데이터스토어 — `public_layer` 스키마 PostGIS 접속 정보 정리

수정 | geoserver_modules/data_dir/workspaces/ggnr/postgres_public_layer/*/featuretype.xml, layer.xml
 public_layer 레이어 카탈로그 — 바운딩박스·스타일 참조·레이어 ID 정리

수정 | geoserver_modules/data_dir/styles/*.xml, *.css
 레이어 스타일 카탈로그 — 스타일 색상·기본 스타일 매핑 정리


## 테스트

1. 레이어 관리에 진입하면 목록 탭이 열리고 상단에 «SHP 업로드»·«Excel 업로드»가 보여야 한다.
2. SHP 폴더 업로드 1단계에서 좌표계 후보와 미리보기 지도가 나오고 선택 결과가 반영되어야 한다.
3. SHP 폴더 업로드 위저드의 1~4단계 제목과 진행 표시가 업무 순서와 맞아야 한다.
4. 2단계에서 Table·Layer·Style·Define 누락 행이 표시되고 «시작» 후 누락 항목만 생성되어야 한다.
5. 키필드가 맞지 않으면 위저드·속성 설정에서 키필드만 편집·저장할 수 있어야 한다.
6. 스키마가 맞지 않으면 3단계로 넘어가지 않고 비고와 안내가 보여야 한다.
7. 3단계에서 «반영·유지»를 선택해도 DB 건수는 바로 바뀌지 않아야 하며, 4단계 완료 후 반영과 이력 기록이 남아야 한다.
8. 3단계 진행 중 위저드를 닫으면 확인 팝업 후 미반영 정합성 비교·선택이 삭제되고 진행 중 이력이 정리되어야 한다.
9. SHP 업데이트 이력 탭에서 배치·레이어별 건수와 결과가 맞게 보여야 한다.
10. Excel 업데이트 이력·Excel 업로드는 탭·버튼 진입만 확인하고, SHP 수준 완성은 후속 과제로 둔다.
11. 목록에서 «이력»을 클릭하면 해당 테이블의 수정 이력이 열려야 한다.
12. 목록의 스타일 범례 미리보기가 GeoServer 스타일과 일치해야 한다.
13. 오류수정 탭에서 레이어·스타일·정의 누락이 보여야 하고 자동 수정이 가능해야 한다.
14. 정합성 내역에서 구분·필드 필터를 적용하면 1페이지 건수와 푸터의 총 건수가 서버 결과와 일치해야 한다.
15. 정합성 목록에서 신규 행이 충돌로 잘못 표시되지 않아야 한다.
16. 정합성 목록·상세에서 변경된 필드가 모두 보여야 한다.
17. 정합성 비교 후 `_sync_` 임시 테이블이 DB에 남지 않아야 한다.
18. SHP 위저드·기타기능 SHP→DB 화면이 Table·Layer·Style·Define 같은 용어를 일관되게 써야 한다.
19. 업로드·정합성 완료 레이어를 지도에서 켜면 화면에 표시되어야 한다.
20. 지도에서 해당 레이어를 클릭하면 속성 패널에 해당 정보가 조회되어야 한다.
21. GeoServer 기동 후 `postgres_layer`·`postgres_public_layer` 데이터스토어가 연결되어야 한다.

## 프롬프트

docs/brief/20260630_2231_레이어업로드절차.md
docs/plan/20260706_0755_레이어관리.md
docs/plan/20260721_1036_SHP위저드미완료취소.md
docs/plan/20260721_1118_SHP위저드4단계지연반영.md
