# 결과보고서

2026-08-07 | jdong | 필지분석 v7 최종 통판 (plan 기준 · 현재 소스)

## 목적

지도에서 분석 범위를 정한 뒤 선택한 항목(토지·소유·지목·이용계획·건축물대장·기본도·시설 등)을 한 화면 보고서에서 확인하도록 한다.

필지·건축 연계는 **이중화** ※운영(행정망)과 개발·시연(외부망)을 같은 화면으로 쓰기 위해, 상황에 따라 데이터 출처를 나누는 규칙이다. **필지분석 보강**과 **우클릭 필지정보**가 같은 규칙을 쓴다.

## 요약

사이드바에서 «필지분석»을 켜면 먼저 분석 영역을 고른다. 지도에 사각·다각·원으로 그리거나, 읍·면·동·리 행정경계를 고를 수 있다. 영역을 확정한 뒤에야 좌측 패널에서 분석 항목을 고르고 «분석»을 누른다. 결과는 가운데 보고서형 모달(왼쪽 목차·오른쪽 본문)로 본다. 메뉴를 켤 때 설정 시군구가 화면에 크게 잡히고, 그린 도형이 사업 구역을 벗어나면 경고한 뒤 확정을 막아 다시 그리게 한다.

«분석»을 누르면 우리 DB에서 필지 수·면적·소유·지목 통계를 먼저 구해 모달을 바로 연다. 그다음 토지 목록·보강·건축물대장·이용계획 등은 **100건씩** 끊어 이어서 채운다. 로드 중에는 섹션 상단 진행 한 줄과 표 푸터 스피너로만 상태를 보여, 같은 「불러오는 중」문구가 여러 곳에 겹치지 않게 한다.

서버 토지 보강·이용계획·우클릭 탭은 **행망만** 담당하고, 브이월드 ※공간정보 오픈플랫폼 는 **브라우저 점진 로딩**에서만 이어 붙인다.

### 데이터는 어디서 오나 (이중화)

토지·소유·공시지가 보강은 **행망을 쓰는 경우**와 **행망을 쓰지 않는 경우**가 처음부터 갈린다. 둘을 한 문장에 섞지 않는다.

**A. 행망을 쓰는 경우 (`GGNR_ENV`가 dev가 아님 — demo·prod 등 운영)**

1. 먼저 KRAS ※토지행정망(KRAS) 으로 토지·소유를 가져온다.
2. 이어서 KOREPS ※부동산공시가격 조회망(KOREPS, 조회코드 KOREPS00011) 으로 공시지가를 채운다.
3. 위 단계에서 **못 채운 필지**는 브라우저에서 브이월드 ※공간정보 오픈플랫폼 로 보강한다.

**B. 행망을 쓰지 않는 경우 (`GGNR_ENV=dev`, 로컬 `npm run dev -- … dev`)**

1. KRAS·KOREPS는 **호출하지 않는다.**
2. 보강은 **브이월드만** 사용한다.

**건축물대장**

- 세움터 ※건축행정 적재 DB(`seum`) 표제를 PNU로 먼저 조회한다. (실시간 세움터 웹 API가 아님. 공동이용센터→ESB ZIP→스케줄 적재.)
- 없거나 실패·타임아웃이면: 공공데이터포털 ※국가 Open API 허브 건축물대장 표제부(`getBrTitleInfo`).
- 포털은 **필지 1건 = 요청 1번**이다. 응답은 JSON이며, 바깥 `response` 묶음이 있는 형태와 없는 형태를 모두 읽는다.
- 초당 호출 한도(코드 23·HTTP 429)가 나면 **남은 필지 호출을 바로 멈추고** 안내한다. 연결실패·타임아웃(코드 04·05·HTTP 502·503)은 한도와 구분해 재시도한다.
- 대지위치·지번이 비면 PNU로 행정명·번지 폴백. 도로명은 원천(세움·포털)만.

**우클릭 필지정보 (같은 연계 규칙)**

- 건축물대장: 필지분석과 같은 서버 경로(세움→포털). 탭을 열 때 **PNU 기준으로 한 번**만 조회한다(지번이 나중에 채워져도 재호출하지 않음).
- 건축인허가: 세움 인허가 → 없으면 포털 건축·주택 인허가. JSON·XML 모두 파싱. 연결실패 시 재시도 후 「연결이 불안정합니다」 안내.

**시설·기본도**

- 시설 통계·목록은 우리 DB·레이어 정의, 지도 그림은 GeoServer ※지도 서버 WMS.

### 프로젝트 runtime 설정 (연계)

행망 **키·IP·포트·시군구코드**는 `*.runtime.env`에 **미리 적어 둔다**.

| 구분 | 내용 |
| -------------------- | ------------------------------- |
| `KRAS_*`·`KOREPS_*`·`SGG_CODE` | 접속정보·키. 사업별 runtime에 보관 |
| 행망 호출 여부 | `GGNR_ENV`가 **dev가 아닐 때만**(demo·prod). `npm run dev -- <project> dev`면 키가 있어도 브이월드만 |
| 실패·누락 시 | 브이월드·공공데이터포털로 이어감 |
| 세움 DB | 세움 적재 DB 우선 → 실패·타임아웃 시 포털 |
| 포털 키 | `DATA_PORTAL_KEY` (건축물대장·인허가) |

**영양 외부 개발 PC**

- `npm run dev -- build_yy dev` → 행망 미호출. 보강은 브이월드.
- runtime에 KRAS·KOREPS 키는 미리 적어 둬도 dev 실행이면 사용하지 않음.

### 묶음·동시·타임아웃 (소스 기본값)

| 구분 | 기본 | 설명 |
|------|------|------|
| 점진 로딩 묶음 | **100필지** | 표·보강을 이 단위로 이어서 채움 (화면 끊기용) |
| 토지 연계 동시 | **8건** | KRAS·KOREPS·브이월드 보강 |
| 건축물대장 동시 | **3건** | 공공데이터포털 초당 한도 완화 |
| 건축물대장 시작 간격 | **200ms** | 요청을 띄우는 최소 간격(우클릭 1건은 간격 없음) |
| 단건 대기 | **토지 8초 / 건축 10초** | 느린 한 건만 끊고 이미 채운 표는 유지 |
| 건축 일시장애 재시도 | **최대 2회** | 연결실패·타임아웃만. 한도(429)는 재시도하지 않고 중단 |

**100건:** 행망·포털에 한 번에 100건을 넣는 값이 아니다. **필지 1건 = 요청 1번**이고, 묶음 안에서는 위 동시·간격만 돌린다. 100은 표를 얼마나 자주 이어서 채울지 정한 화면 묶음이다.

묶음·동시·간격·단건 대기는 행망·포털이 느려도 화면이 멈춘 것처럼 보이지 않게, 또 포털 초당 한도에 덜 걸리게 둔 **운영 기본값**이다. (초당 한도가 더 빡세면 동시·간격을 더 줄여야 할 수 있다.)

### 느릴 때·일부만 실패할 때

필지가 많으면 **100필지씩** 나눠 처리한다. 어떤 묶음에서 망이 끊기거나 시간이 초과되면 그 묶음만 비워 두고, 이미 채워진 표·통계는 **유지**한다.

연계가 일부라도 실패하면 모달에 **한 줄**만 띄운다. «연계가 끊기거나 시간 초과로 일부 항목이 비어 있습니다.» «미상»·빈 칸이 그 상태의 표시다.

건축물대장이 포털 초당 한도에 걸리면 건축물대장 섹션에만 «공공데이터포털 호출이 제한되어…» 안내를 둔다. 연결 불안정만 있고 행이 없으면 «연결이 불안정합니다…»로 구분한다.

### 결과 화면

소유자·지목 현황은 구분별 색칠 지도와 통계 표를 같이 보여 주고, 기본도·시설은 지도 캡처로 보여 준다. 테마 지도와 캡처는 휠·드래그·줌 버튼 없이 고정 그림처럼 보이며, 그 설정은 둘이 같이 쓴다. 시설(도로·하천·기타 등) 캡처는 **해당 섹션 표에 나온 레이어만** 그린다(그룹 publish 전체 아님).

테마 지도 색·범례는 토지현황 **보강 값**으로 칠한다(서버는 필지 번호·도형만 주고 색은 화면에서 맞춘다).

**진행 표시**

- 섹션 상단: 진행 문구 + 스피너 **한 줄**(「불러오는 중」을 여러 줄로 반복하지 않음).
- 표 하단 푸터: 로드 중일 때 스피너·건수.
- 토지현황은 조회와 소유·공시 연계를 **한 줄로** 합친다.

**범례·«그 외» 묶음 (소유자·지목 공통)**

분석 필지가 **2,000건 이하**이면 나온 소유구분·지목을 **전부** 각각 색칠·범례에 낸다.  
분석 필지가 **2,000건을 넘으면** 면적이 큰 순으로 **상위 10개 구분**만 지도·범례에 개별 색으로 두고, 나머지는 **«그 외»**로 묶어 한 색으로 칠한다. 통계 표에는 구분 모두 나온다.  
이때 소유자·지목 현황 **제목 아래**에만 «2,000건 초과·그 외» 안내를 작은 글씨로 표시한다(이하일 때는 안내 없음).

**한계·후속:** 위 2,000건·상위 10구분은 측정 근거가 아닌 실무 상한이다. 실제 영역 규모·응답·화면 반응에 따라 **필지 수 상한과 범례(상위 구분) 수 조정이 필요**하다. 포털 초당 한도에 따라 건축물 동시·간격도 조정이 필요할 수 있다.

## 파일수정

재사용 | src/app/(pages)/map/map-layout-client.tsx
 `ParcelAnalysisProvider` — 필지분석 상태 트리
 `ParcelAnalysisOrchestrator` / `ParcelAnalysisMapSidePanel` — shell 연동

재사용 | src/app/(pages)/map/_mapComponents/map-sidebar.tsx
 `parcelAnalysis` — 메뉴로 모드 토글

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/ParcelAnalysis.shell.tsx
 `ParcelAnalysisOrchestrator` — 영역 모달·분석 중·결과 모달·구역 이탈 배너·도형 편집바
 `ParcelAnalysisMapSidePanel` — 좌측 영역 요약·항목·분석

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/parcelAnalysisContext.tsx
 `handleAnalyze` — DB 통계 후 모달 오픈·점진 로딩
 `cancelAnalyze` — 분석 중단·재설정

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/ParcelAnalysis.area.tsx
 `useParcelAnalysisDraw` — 사각·다각·원
 `useParcelAnalysisMapZoom` — 시군구 중앙 확대
 `useParcelAnalysisAreaLayer` — 영역 표시
 `useParcelAnalysisDrawToolbarPosition` — 편집바 위치

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/ParcelAnalysis.boundary.tsx
 `ParcelAnalysisBoundaryPicker` — 읍·면·동·리
 `useParcelAnalysisSigunguBoundary` — 시군구 참고 경계

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/ParcelAnalysis.items.tsx
 `PARCEL_ANALYSIS_GROUPS` — 기본도·건축물·필지·시설(동적)
 `ParcelAnalysisItemSelector` — 항목 선택

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/ParcelAnalysis.resultModal.tsx
 `ParcelAnalysisResultModal` — 목차·섹션·표·진행·연계 부분 실패 안내
 `SectionProgressLine` — 섹션당 진행 한 줄·스피너 1개
 `TableProgressFooter` — 표 하단 로드 표시
 `formatLandSectionProgress` — 토지 조회·연계를 한 줄로 합침

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/parcelAnalysis.result.ts
 `ParcelAnalysisResult` — 결과 조립 타입
 `runParcelAnalysisProgressiveLoad` — 서버 행망 보강 후 브라우저에서 브이월드·이용계획 이어 채움. 청크 작업이 끝날 때까지 로드 중 유지
 `linkageNotice` — 연계 일부 실패 시 확정형 한 줄 안내
 `buildingLedgerNotice` — 건축물대장 포털 한도·연결 안내

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/ParcelAnalysis.mapCapture.tsx
 `ParcelAnalysisMapCapture` — 기본도·시설 WMS 캡처(고정 표시)

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/ParcelAnalysis.themeMap.tsx
 `ParcelAnalysisThemeMap` — 소유·지목 색칠·범례. 휠·드래그·줌 버튼 없음. 보강 필지 값으로 색
 `ParcelAnalysisThemeLegend` — 구분 색칩. 2,000필지 초과 시 상위 10+«그 외»

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/parcelAnalysis.mapStyle.ts
 `createParcelAnalysisStaticMapOptions` — 캡처·테마 공통 상호작용 비활성
 `createParcelAnalysisBasemapSource` — 브이월드 타일 실패 시 단색 폴백
 `PARCEL_ANALYSIS_AREA_STYLE` — 영역·결과 지도 스타일

추가 | src/app/(pages)/map/_mapContents/parcelAnalysis/parcelAnalysis.types.ts
 `isLargeParcelAnalysisArea` — 대형 영역 사전 확인
 `DrawProjectScope` — 사업 구역 안·일부 밖·완전 밖

추가 | src/lib/parcelAnalysisTheme.ts
 `PARCEL_ANALYSIS_BUILDING_CONCURRENCY` — 건축물대장 동시 3
 `PARCEL_ANALYSIS_BUILDING_GAP_MS` — 요청 시작 간격 200ms
 `PARCEL_ANALYSIS_LINKAGE_CONCURRENCY` / `*_TIMEOUT_MS` — 토지 연계 동시·타임아웃
 `PARCEL_THEME_MAP_FULL_COLOR_LIMIT` — 전면 색칠 상한 2,000필지
 `PARCEL_THEME_MAP_TOP_CATEGORY_COUNT` — 상한 초과 시 지도·범례 상위 10구분
 `buildThemeCategoriesFromParcels` — 보강 값으로 범주·그 외 묶음

추가 | src/lib/parcelAnalysisCatalog.ts
 시설 그룹·레이어 카탈로그

추가 | src/lib/parcelLandNormalize.ts
 `normalizeFromKrasRow` / `normalizeFromVworldParts` — 행망·브이월드 공통 형식
 `applyEnrichmentToLandRows` — 토지현황 행 병합
 `buildPnuQueryParams` — PNU→공공데이터 쿼리(건축·인허가 공용)
 `mapKrasToParcelLandInfoTab` / `formatParcelLandLinkageField` — 우클릭 탭·화면 표시 라벨

추가 | src/lib/vworldParcelLandClient.ts
 `fetchLandInfoConfig` — 지도 클라이언트 키
 `fetchVworldParcelLandEnrichmentBatch` / `fetchVworldLandUseZonesBatch` — 점진 로딩 브이월드
 `fetchVworldParcelTabData` / `fetchVworldLatestOfficialLandPrice` — 우클릭 탭·공시 폴백

추가 | src/lib/buildingLedgerFetch.ts
 `fetchBuildingLedgersByPnus` — 세움 DB 우선 → 포털. JSON `response` 유무 모두 파싱
 `mapPoolWithStartGap` — 동시 3 + 시작 간격. 한도 나면 나머지 건너뜀
 `isDataPortalQuotaMessage` — 초당·일일 한도(연결실패 05는 제외)
 `BUILDING_LEDGER_PORTAL_QUOTA_NOTICE` / `BUILDING_LEDGER_PORTAL_TIMEOUT_NOTICE` — 한도·연결 안내 구분
 `BuildingLedgerFetchDebug` — 사유별 요약 로그용

추가 | src/service/mapAnalyseService.ts
 `analyzeParcels` — 영역 교차 필지 DB 통계
 `listAnalyzeLandRows` — 면적 큰 순 목록 페이지
 `enrichParcelLandRows` — 서버 행망(KRAS·KOREPS) 보강만 중계
 `fetchBuildingLedgersForParcels` / `listAnalyzeThemeMapFeatures` — 건축물대장·테마 도형

추가 | src/service/landLinkageService.ts
 `enrichParcelLandsByPnus` — KRAS 후 KOREPS 공시 보강(서버, XML 파싱 포함)
 `fetchLandUseZonesByPnus` — 이용계획 행망만(서버)
 `fetchParcelLandInfoTab` — 우클릭 탭 행망만(브이월드는 브라우저)

추가 | src/service/seumService.ts
 `fetchSeumBuildingLedgerByPnu` / `fetchSeumBuildingLedgersByPnus` — 세움터 DB 표제
 `fetchSeumPermitRowsByPnu` — 건축·주택 인허가(세움)

추가 | src/app/(pages)/map/_mapComponents/parcelLandLinkageUi.tsx
 `ParcelLandLinkageSourceText` / `LandLinkageLegend` — 필지분석·우클릭 연계 출처 UI(확인용 플래그)

수정 | src/app/(pages)/map/_mapComponents/landInfo/api.ts
 `fetchParcelTabData` — 브이월드 폴백을 공용 클라이언트로 위임
 `fetchBuildingLedgerRows` — 필지분석과 동일 건축물대장 경로·디버그 로그
 `fetchPermitRows` — 세움→포털. JSON·XML 파싱. 연결실패 재시도·안내
 `parsePortalRows` / `fetchPortalTextWithRetry` — 인허가 포털 응답·재시도

수정 | src/app/(pages)/map/_mapComponents/landInfo/LandInfoPanelContent.tsx
 필지정보 패널 — 건축물대장·인허가 안내. 건축물대장은 PNU만으로 재조회(지번 state 재실행 방지)

수정 | src/app/(pages)/map/_mapComponents/OpenLayersMap.tsx
 우클릭 주소 — 지번 전체주소·도로명 표시 시 시·도·시·군·구 제거 규칙

수정 | src/app/(pages)/map/_mapComponents/addressSearch/vworldAddressSearch.ts
 도로명·지번 재조회·표시 정규화

수정 | src/app/api/public-data/building/route.ts
 건축·인허가 포털 프록시 — 응답·상태 로그

수정 | src/service/configService.ts
 `getLandLinkageConfig` — `GGNR_ENV`가 dev가 아닐 때만 행망·공시 호출. 세움 DB 우선

수정 | src/service/index.ts
 `seumService` / `seumPermitService` — 세움 서비스 등록(인허가 API명 호환)

수정 | src/config/projects/build_yy.runtime.env
 `KRAS_*`·`KOREPS_*`·`DATA_PORTAL_KEY` — 행망·포털 키·접속정보 미리 기입

## 테이블 수정

해당 없음

## 테스트

1. 사이드바 «필지분석» 선택 시 영역 지정 모달만 열리고 좌측 패널은 없어야 한다.
2. 모달 X·취소(영역 미확정) 시 모드가 종료되어야 한다.
3. 도형·행정경계로 영역 확정 후 좌측 패널·지도 영역 표시가 나타나야 한다.
4. 사업 구역 이탈·일부 이탈 시 경고가 보여야 한다.
5. «분석» 후 결과 모달에 필지 수·면적·소유·지목 통계가 먼저 보여야 한다.
6. 토지현황이 «N / M건»으로 이어 채워지고, 소유·공시지가 보강이 반영되어야 한다.
7. `GGNR_ENV`가 dev가 아니면(demo·prod) 필지분석 토지 보강이 KRAS·KOREPS를 쓰고, 못 채운 필지·이용계획·우클릭은 브이월드로 이어져야 한다.
8. `GGNR_ENV=dev`(로컬 dev 실행)이면 KRAS·KOREPS 없이 브이월드만 써야 한다.
9. 토지이용계획·건축물대장·시설 항목 선택 시 해당 섹션이 채워져야 한다.
10. 건축물대장이 있는 필지는 포털·세움 결과가 표에 나와야 하고, JSON만 오는 응답도 비어 보이지 않아야 한다.
11. 필지가 많을 때 포털 초당 한도에 걸리면 건축물대장 섹션에 한도 안내가 나오고, 터미널에는 사유별 요약 로그가 한 번에 나와야 한다.
12. 로드 중 진행은 섹션 상단 한 줄과 표 푸터에만 보이고, «불러오는 중»이 여러 줄로 겹치지 않아야 한다.
13. 소유자·지목 섹션에 색칠 지도·범례·표가 있고, 휠·드래그·줌 버튼이 없어야 한다.
14. 필지 2,000건 이하이면 소유·지목 구분이 범례에 각각 나오고, 2,000건 초과이면 상위 10구분만 개별·나머지는 «그 외»로 묶여야 한다.
15. 테마 지도 색·범례가 토지현황 보강 결과(표)와 맞아야 한다.
16. 기본도·시설 캡처도 고정 표시여야 한다.
17. 연계가 일부 실패해도 표·통계는 유지되고, 모달에 «연계가 끊기거나 시간 초과로 일부 항목이 비어 있습니다» 한 줄만 나와야 한다.
18. 우클릭 «건축물대장»은 같은 필지에서 지번만 바뀌어도 두 번 호출되지 않아야 한다.
19. 우클릭 «건축인허가»는 포털 JSON 응답이 표에 나오고, 연결실패 시 안내가 나와야 한다.
20. 다른 지도 메뉴 전환 시 필지분석 잔여 UI가 없어야 한다.

## 프롬프트

docs/brief/20260630_2211_필지분석.md
docs/plan/20260702_1042_필지분석.md
docs/plan/20260706_0856_필지연계이중화.md
