# 결과보고서

2026-08-21 | JungWooojin | DB연결초과

## 목적

목록이 연결 한도 초과로 실패하지 않게, 앱이 쓰는 DB 연결 수를 줄인다.

## 요약

오류는 테이블이 없어서가 아니라 DB 연결이 100개를 넘어서다. 지도 서버·여러 DBeaver가 유휴 연결을 많이 잡고 있다. 앱은 연결을 최대 5개만 쓰도록 바꿨다. 안 쓰는 DBeaver 창은 닫아야 한다.

## 파일수정

수정 | src/database/db.ts
 `getPool` — 개발 중 다시 불러와도 연결 풀을 새로 만들지 않음. 한 프로세스당 최대 5개, 잠시 쉬면 반환

## 테이블 수정

해당 없음

## 테스트

1. 안 쓰는 데이터베이스 도구 창을 닫은 뒤 접도구역 건축물 목록이 열려야 한다.
2. 목록 오류에 연결 한도 초과가 다시 나와서는 안 된다.

## 프롬프트

docs/brief/ — 해당 없음
docs/plan/ — 해당 없음
