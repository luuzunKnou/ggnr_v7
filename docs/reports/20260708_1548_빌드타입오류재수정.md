# 결과보고서

2026-07-08 | 200BingSu | 빌드 타입 오류 재수정

## 목적

standalone 제거 후 `npm run build` 시 다시 발생한 TypeScript 오류를 이전과 동일하게 수정해 프로덕션 빌드가 통과하도록 한다.

## 요약

다른 파일의 이전 수정분은 유지되어 있었고, `LayerRowGeomEditHandler.tsx` 3건만 되돌아가 있어 재적용했다. `tsc`·`npm run build` 모두 성공했다.

## 파일수정

수정 | src/app/(pages)/map/_mapComponents/layerRowEdit/LayerRowGeomEditHandler.tsx
 `writeGeometryObject` 단언 — `unknown` 경유 후 `Record`로 변환
 `layerRowGeomFeatureStyle` — `FeatureLike` 매개변수로 OpenLayers 스타일 타입 정합
 `subtractParcelFromParentGeom` — Context ref 시그니처에 맞는 필지 매개변수 타입

## 테이블 수정

해당 없음

## 테스트

1. `npx tsc --noEmit` 오류 0이어야 한다.
2. `GGNR_PROJECT=build_yy`, `GGNR_ENV=dev` 환경에서 `npm run build`가 성공해야 한다.

## 프롬프트

brief: —
plan: docs/plan/20260707_1808_빌드타입오류수정.md
