# 결과보고서

2026-09-21 | dev.strcat | 알림→도로점용·국공유지 이동 오류

## 목적

건설행정 메뉴에 둔 도로점용·국공유지(점용대장) 알림을 눌렀을 때 해당 대장으로 이동되지 않던 문제를 고친다.

## 요약

알림 표시 기준을 데이터 접두가 아니라 **현재 시스템 부서업무(메뉴)에 그 기능이 있는지**로 맞췄다. 도로점용·국공유지가 건설행정에만 있으면 건설에서만 보이고(목록에서는 도로/국공유로 구분), 도로행정 부서업무에 없으면 도로에서는 안 뜬다. 클릭 시에도 해당 대장·점사용료 패널로 연다.

## 파일수정

수정 | src/service/bizNotifService.ts
 `resolveNotifSystemScope` — 부서업무에 기능이 있는 시스템으로 알림 소속 맞춤. 메뉴에 없으면 소속 비움
 `listMyBizNotifications` — 알림에 열 패널 식별값(serEng)을 함께 내려줌

수정 | src/app/(pages)/map/_mapContents/bizNotif/bizNotifStore.ts
 `ProtoNotifItem.serEng` — 알림 항목에 열 패널 식별 필드 추가

수정 | src/app/(pages)/map/_mapContents/bizNotif/bizNotifClient.ts
 `notifMatchesActiveSystem` — 현재 시스템 부서업무에 serEng 가 있을 때만 표시
 `refreshBizNotifs` — 메뉴 목록을 넘겨 필터에 반영

수정 | src/app/(pages)/map/_mapComponents/UsageDataAsNotifBootstrap.tsx
 `UsageDataAsNotifBootstrap` — 시스템 목록을 읽어 알림 필터에 메뉴 반영

수정 | src/app/(pages)/map/_mapContents/prototypes/UserAccountProtoPanel.tsx
 알림 새로고침 — 메뉴 목록과 함께 갱신
 `onOpenFee` — 알림 항목 전체를 넘기도록 변경

수정 | src/app/(pages)/map/_mapContents/prototypes/UserAccountProtoNotifTab.tsx
 `openItem` — 점사용료도 알림 항목 단위로 열기

수정 | src/app/(pages)/map/map-layout-client.tsx
 `onOpenLedger` — 알림의 점용 종류(도로·국공유 등)로 패널을 열고, 메뉴에 없으면 해당 시스템으로 전환
 `onOpenFee` — 현재 시스템이 아니라 알림의 점사용료 종류로 패널 오픈

## 테이블 수정

해당 없음

## 테스트

1. 건설행정 부서업무에 도로점용·국공유지가 있으면 알림에 둘 다 보여야 하고, 이름·제목으로 구분되어야 한다.
2. 도로행정 부서업무에 도로점용·국공유지가 없으면 도로행정 알림에 해당 건이 나오지 않아야 한다.
3. 건설행정에서 도로점용 알림 클릭 시 도로점용 목록·상세가 열려야 한다.
4. 건설행정에서 국공유지 알림 클릭 시 국공유지 점용 목록·상세가 열려야 한다.
5. 도로·국공유지 점사용료 알림도 부서업무가 있는 시스템에서만 보이고, 클릭 시 각각 맞는 패널이 열려야 한다.
6. 하천점용·울진점용 알림은 하천 부서업무 기준으로 기존과 같이 동작해야 한다.

---

2026-09-21 | dev.strcat | 타입오류 수정

## 목적

빌드·타입 검사에서 난 OpenLayers 클릭 리스너·범례 PNG 응답 타입 오류를 제거한다.

## 요약

지도 단일 클릭 핸들러 타입을 기존 레이어와 같이 맞추고, 범례 API는 Node Buffer 대신 Uint8Array로 응답하도록 바꿨다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/aerialView/useOrthoExtentBboxLayer.ts
 `onClick` 등록·해제 — OpenLayers 리스너 타입 불일치 해소

수정 | src/app/api/geoserver/legend/route.ts
 `emptyLegend` · `legendPng` — NextResponse 본문을 Uint8Array로 전달

## 테이블 수정

해당 없음

## 테스트

1. 타입 검사에서 해당 두 파일 오류가 없어야 한다.
2. 항공영상 범위 레이어 클릭이 기존과 같이 동작해야 한다.
3. 지도 범례 이미지가 정상 표시되어야 한다.

---

2026-09-21 | dev.strcat | 도로대장 종류코드·목록표기

## 목적

울진은 도로종류 코드표를 따로 쓰고, 목록·상세 괄호는 호선·구간 한글 표기로 맞춰 찾기 쉽게 한다.

## 요약

`build_uj`만 울진용 도로종류(1504 광역시도 등)를 쓰고, 영양·경산 등은 기존 표를 유지한다. 목록 괄호는 `(305호선 1구간)` 형태이며, 제목에 이미 구간이 있으면 `(8호선)`만 붙인다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/road/roadLedger/roadLedgerFormat.ts
 `ROAD_RANK_CODE_KO_ULJIN` — 울진용 도로종류·뱃지색
 `formatRoadLedgerParenRoadNoSectOnly` — 호선·구간 표기, 제목 구간 중복 생략
 `formatRoadLedgerDetailTitleParen` — 상세 괄호도 동일 규칙

수정 | src/app/(pages)/map/_mapContents/road/roadLedger/RoadLedgerListPanel.tsx
 목록 행 — 기동 프로젝트로 종류 뱃지·괄호 표기

수정 | src/app/(pages)/map/_mapContents/road/roadLedger/RoadLedgerDetailPanel.tsx
 상세 제목·도로의종류 — 프로젝트별 코드표 적용

## 테이블 수정

해당 없음

## 테스트

1. 울진에서 도로종류 1507이 «군도», 1505가 «지방도»로 보여야 한다.
2. 영양·경산에서 1506이 «군도», 1504가 «지방도»로 보여야 한다.
3. 목록이 «가정기선 (305호선 1구간)»처럼 보여야 한다.
4. 제목에 구간이 있으면 «도곡-죽파 3구간 (8호선)»처럼 괄호에 구간이 중복되지 않아야 한다.
5. 상세 제목 괄호도 호선·구간 표기여야 한다.

---

2026-09-21 | dev.strcat | 도로대장 호선구간검색

## 목적

목록 검색에서 «20호선»·«1구간»처럼 화면 표기와 같은 말로도 찾을 수 있게 한다.

## 요약

검색어의 호선은 노선번호, 구간은 구간 필드에 맞춰 조회한다. 둘 다 넣으면 함께 만족하는 행만 나온다.

## 파일수정

수정 | src/service/roadLedgerService.ts
 `buildRoadLedgerKeywordClause` — 호선·구간 토큰 파싱 후 노선번호·구간 매칭
 `roadLedgerNumericFieldMatchSql` — 선행 0이 있어도 숫자로 동일 비교

## 테이블 수정

해당 없음

## 테스트

1. «20호선» 검색 시 노선번호 20인 행이 나와야 한다.
2. «1구간» 검색 시 구간 1인 행이 나와야 한다.
3. «305호선 1구간» 검색 시 둘 다 맞는 행만 나와야 한다.
4. 도로명만 검색하는 기존 동작은 유지되어야 한다.

---

2026-09-21 | dev.strcat | 시설관리클릭·도로망굵기·호선검색

## 목적

시설관리에서 지도 클릭 시 상세가 열리게 하고, 도로망 확대 시 선 굵기를 키우며, 호선 검색 오탐을 줄인다.

## 요약

시설관리는 데이터조회와 같이 지도 식별 결과를 목록·상세로 넘기도록 했다. 도로망 CSS는 축척 약 120m부터 굵기 2배. «5호선» 검색은 노선번호 완전일치만 사용해 15호선이 나오지 않게 했다.

## 파일수정

수정 | src/app/(pages)/map/_mapComponents/OpenLayersMap.tsx
 지도 식별 — 시설관리(roadInfra) 열림 시에도 listView·상세 오픈

수정 | src/service/roadLedgerService.ts
 `buildRoadLedgerKeywordClause` — 호선·구간은 숫자 완전일치만 (부분일치 제거)

수정 | geoserver_modules/data_dir/styles/rdl_*_ls.css (도로망 10종)
 `[@sd <= 4500]` — 축척 약 120m 이하에서 stroke-width 2배

## 테이블 수정

해당 없음

## 테스트

1. 시설관리만 연 뒤 시설 레이어 클릭 시 우측 상세·목록이 열려야 한다.
2. 도로망도에서 축척 120m 부근으로 확대하면 선이 더 굵게 보여야 한다.
3. «5호선» 검색 시 15호선이 나오지 않고 5호선만 나와야 한다.
4. «305호선 1구간» 검색은 기존처럼 동작해야 한다.

## 프롬프트

—
—

---

2026-09-21 | dev.strcat | 도로대장 매설물도·지하매설물 매핑 제외

## 목적

도로대장 상세의 매설물도·지하매설물(지하시설물) 레이어 연동을 당분간 끈다.

## 요약

매설물도 버튼에 묶여 있던 지하매설물 레이어와, 부대시설에 중복 포함돼 있던 동일 레이어를 매핑에서 뺐다. 버튼은 남지만 레이어를 켜지 않는다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/road/roadLedger/roadLedgerDocLayerMap.ts
 `ROAD_LEDGER_DOC_LAYERS` — 매설물도 레이어 목록 비움, 부대시설에서 지하매설물 제외
 `ROAD_LEDGER_DOC_LABELS_WITH_LAYER_COUNT` — 매설물도 건수 표시 대상에서 제외

수정 | src/app/(pages)/map/_mapContents/road/roadLedger/RoadLedgerDetailPanel.tsx
 매설물도 전용 비활성·건수 분기 — 레이어 없음과 동일하게 버튼만 비활성

## 테이블 수정

해당 없음

## 테스트

1. 도로대장 상세에서 매설물도를 눌러도 지도에 지하매설물 레이어가 켜지지 않아야 한다.
2. 부대시설을 켜도 지하매설물만 따로 켜지지 않아야 한다.
3. 주요시설·안전시설·배수시설 등 다른 대장 버튼은 기존처럼 레이어가 켜져야 한다.
4. 지도 우측 «지하시설물» 패널은 도로대장과 무관하게 동작해야 한다.

## 프롬프트

—
—

---

2026-09-21 | dev.strcat | 옹벽 라인 스타일

## 목적

옹벽 지오메트리가 선(멀티라인)인데 면 스타일로 그려지던 문제를 고친다.

## 요약

옹벽 스타일을 폴리곤에서 라인(이중 stroke)으로 바꿨다. 테이블 도형 유형도 선으로 맞춤.

## 파일수정

수정 | geoserver_modules/data_dir/styles/f9047224.css
 라인 stroke — 흰색·남색 이중선 (면 fill 제거)

수정 | geoserver_modules/data_dir/styles/f9047224.sld
 `LineSymbolizer` — CSS와 동일 라인 심볼

수정 | src/config/defineLayer/tables.json
 옹벽 도형 유형 — 폴리곤→선

## 테이블 수정

해당 없음

## 테스트

1. 도로대장 배수시설에서 옹벽을 켜면 선으로 보여야 한다.
2. 면으로 채워지지 않아야 한다.
3. 석축·절개 등 다른 배수시설 라인은 기존처럼 보여야 한다.

## 프롬프트

—
—
