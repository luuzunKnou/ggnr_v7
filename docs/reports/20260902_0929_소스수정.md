# 결과보고서

2026-09-02 | dev.strcat | —

## 목적

대상여부 검토와 설계실무요령 자료의 글자 크기를 맞춘다. 제목은 12픽셀, 본문은 11픽셀이다.

## 요약

목록·상세에서 절차명·자료명·구역 제목은 12픽셀, 기준·법령 조문·비고·부가 설명은 11픽셀로 통일했다. 검토 목록 부가줄이 더 작거나 자료 목록 제목이 더 크던 차이를 없앴다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/road/roadWorkHandbook/RoadWorkHandbookListPanel.tsx
 `filteredProcs` 목록 부가줄 — 기준·시기 안내를 11픽셀로 맞춤

수정 | src/app/(pages)/map/_mapContents/road/roadWorkHandbook/roadWorkHandbookUi.tsx
 `HandbookMaterialListButton` / `HandbookFileCardInner` — 자료·파일 제목 12픽셀, 출처·장 안내는 11픽셀
 `ProcedureDetailCard` / `ProcedureGuideCard` / `MaterialInfoCard` — 구역 제목 12픽셀, 본문·값 11픽셀

수정 | src/app/(pages)/map/_mapContents/road/roadWorkHandbook/RoadWorkHandbookDetailPanel.tsx
 상세 상단 제목 — 12픽셀

## 테이블 수정

해당 없음

## 테스트

1. 대상여부 검토 목록에서 절차명은 제목 크기, 아래 기준 안내는 본문 크기여야 한다.
2. 설계실무요령 자료 목록에서 자료명은 제목 크기, 장 안내는 본문 크기여야 한다.
3. 검토 상세에서 구역 제목과 법령명·본문 크기가 목록과 같아야 한다.
4. 자료 상세에서 항목 제목과 비고·파일명·출처 크기가 목록과 같아야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

대상여부 검토 목록의 소제목을 키우지 않고 원래 크기로 둔다.

## 요약

검토 목록 아래줄(기준·시기)만 이전 크기로 되돌렸다. 절차명과 자료 탭 글자 크기는 그대로다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/road/roadWorkHandbook/RoadWorkHandbookListPanel.tsx
 `filteredProcs` 목록 소제목 — 11픽셀로 키웠던 기준·시기 안내를 원래 크기로 복구

## 테이블 수정

해당 없음

## 테스트

1. 대상여부 검토 목록에서 절차명 아래 기준·시기 안내가 이전과 같은 크기여야 한다.
2. 설계실무요령 자료 목록 제목·장 안내는 그대로여야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

검토 목록 배지와 사업규모 항목 라벨 글자 크기를 맞춘다.

## 요약

입력필요·별도 등 목록 배지는 10픽셀, 사업규모 신설구간·확장구간 등 항목 이름은 11픽셀로 맞췄다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/road/roadWorkHandbook/RoadWorkHandbookListPanel.tsx
 해당 여부 배지 — 입력필요 등 배지를 10픽셀로 맞춤

수정 | src/app/(pages)/map/_mapContents/road/roadWorkHandbook/roadWorkHandbookUi.tsx
 `OrgBadge` — 별도 등 시행주체 배지를 10픽셀로 맞춤
 `HandbookScaleCard` — 신설구간·확장구간 등 항목 이름을 11픽셀로 맞춤

## 테이블 수정

해당 없음

## 테스트

1. 검토 목록에서 입력필요·별도 배지가 같은 크기여야 한다.
2. 사업규모에서 신설구간·확장구간 이름이 본문 크기여야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

공통 점용대장 수정에서 필지목록의 필지를 삭제하면 점용 도형에서 그 필지 영역이 빠지도록 되돌린다.

## 요약

저장된 필지는 주소만 가지고 있어, 화면에서 삭제해도 점용 도형이 그대로 남는 경우가 있었다. 주소로 지적을 찾지 못하면 이미 저장돼 있는 필지 도형을 그대로 써서 점용 도형에서 빼도록 했다. 영양처럼 주소가 행정리로 저장된 자료에서 특히 문제였다. 또 도형을 뺀 뒤에도 저장 전 옛 도형이 지도에 겹쳐 보여 «안 지워진 것처럼» 보이던 것을 고쳐, 삭제 즉시 결과가 보이게 했다.

영향 받는 기능: 공통 점용대장(하천·도로·국공유지), 울진 하천점용, 도로점용대장, 국공유지대장, 하천점용대장 — 모두 같은 수정 기능을 쓴다.

## 파일수정

수정 | src/app/(pages)/map/_mapComponents/layerRowEdit/useLayerRowEdit.ts
 `parcelHasSubtractGeom` — 필지 행에 도형 또는 지적 번호가 있는지 판단하는 공통 조건으로 분리
 `fetchSavedParcelGeom3857` — 주소·지적 번호로 도형을 못 찾을 때 저장된 필지 행의 도형을 직접 읽어 오는 폴백 추가
 `removeDraftParcel` — 지적 조회 실패 시 저장된 필지 도형으로 대체한 뒤 점용 도형 차감을 요청하도록 변경. 기존에는 조회 실패 시 아무 일도 일어나지 않았음

수정 | src/app/(pages)/map/_mapComponents/layerRowEdit/LayerRowGeomEditHandler.tsx
 `subtractParcelFromParentGeom` — 차감 성공 직후 본표 지도 레이어를 숨겨, 저장 전 옛 도형이 결과를 가리지 않도록 함

## 테이블 수정

해당 없음

## 테스트

1. 공통 점용대장 상세에서 «수정»을 누르면 필지목록이 저장된 그대로 보여야 한다.
2. 필지목록에서 한 필지를 삭제하면 지도의 점용 도형에서 그 필지 영역만 즉시 빠져야 한다.
3. 필지 하나만 있는 건에서 그 필지를 삭제하면 점용 도형이 사라져야 한다.
4. 삭제 후 저장하면 지도에 차감된 도형이 유지되어야 한다.
5. 삭제 후 «취소»하면 도형과 필지목록이 수정 전으로 돌아와야 한다.
6. 도형을 새로 그려 채워진 필지를 삭제할 때도 같은 방식으로 도형이 빠져야 한다.
7. 물건지목록 삭제는 점용 도형에 영향을 주지 않아야 한다.
8. 울진 하천점용·도로점용대장·국공유지대장에서도 필지 삭제 시 도형 차감이 동작해야 한다.
9. 수정을 끝내고 나오면 점용 지도 레이어가 다시 보여야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

공통 점용대장과 울진 하천점용을 부서업무로 열면 점용 본표와 필지 레이어가 같이 켜지게 한다. 울진 하천점용에서도 필지 삭제 시 점용 도형이 같이 빠지도록 맞춘다.

## 요약

부서업무 진입 때 본표만 켜고 필지 레이어는 꺼 두던 처리를 바꿨다. 이제 목록이 열리면 점용대장과 점용대장 필지가 함께 보이고, 물건지만 기존처럼 꺼 둔다. 울진 하천점용도 같은 방식이다. 수정 중에는 저장 전 옛 도형이 결과를 가리지 않게 본표·필지를 잠시 숨기고, 수정을 끝내면 다시 켠다. 필지 삭제 차감은 두 화면이 같은 수정 기능을 쓰므로 울진도 동일하다.

영향 받는 기능: 공통 점용대장, 울진 하천점용.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/occupationLedger/occupationLedgerMapSync.ts
 `ensureOccupationLedgerWmsLayers` — 기본으로 본표와 필지를 함께 켬. 물건지만 따로 켬. 수정 중 숨김은 본표·필지 모두

수정 | src/app/(pages)/map/_mapContents/river/usageDataAs/usageDataAsMapSync.ts
 `ensureUsageDataAsWmsLayersVisible` — 하천점용 본표와 필지를 함께 켬. 물건지만 따로 켬

수정 | src/app/(pages)/map/_mapContents/occupationLedger/OccupationLedgerDetailPanel.tsx
 수정 종료 — 본표·필지 레이어를 다시 켬

수정 | src/app/(pages)/map/_mapContents/river/usageDataAs/UsageDataAsDetailPanel.tsx
 수정 중 레이어 숨김 — 공통 점용과 같이 도형 편집 중에는 본표·필지를 숨기고, 끝나면 다시 켬

수정 | src/app/(pages)/map/_mapComponents/layerRowEdit/LayerRowGeomEditHandler.tsx
 수정 모드 시작 — 본표 레이어를 다시 켜지 않아 옛 도형이 편집 결과를 가리지 않음
 `childLayerName` — 필지 레이어도 함께 숨김·복원

## 테이블 수정

해당 없음

## 테스트

1. 공통 점용대장을 부서업무로 열면 점용 본표와 필지 레이어가 같이 켜져야 한다.
2. 울진 하천점용을 부서업무로 열면 점용 본표와 필지 레이어가 같이 켜져야 한다.
3. 물건지 레이어는 부서업무 진입만으로 켜지지 않아야 한다.
4. 공통 점용에서 필지를 삭제하면 점용 도형에서 그 필지 영역이 빠져야 한다.
5. 울진 하천점용에서 필지를 삭제하면 점용 도형에서 그 필지 영역이 빠져야 한다.
6. 수정 중에는 저장 전 옛 도형이 겹쳐 보이지 않아야 한다.
7. 수정을 취소·저장하면 본표와 필지 레이어가 다시 보여야 한다.
8. 점사용료 레이어 on/off는 이전과 같이 동작해야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

울진 하천점용·공통 점용대장 부서업무에서 예전 조회용 표시와 본표·필지 레이어가 겹치지 않게 한다. 기능이 열리면 본표와 필지만 켜고, 닫히면 끈다.

## 요약

부서업무가 울진 색으로 한 번 더 그리던 처리와, 선택한 점용 도형을 벡터로 덧그리던 처리를 뺐다. 이제 지도에는 본표·필지 레이어만 보인다. 목록을 열면 두 레이어가 켜지고, 목록을 닫으면 꺼진다.

영향 받는 기능: 공통 점용대장, 울진 하천점용.

## 파일수정

수정 | src/lib/occupationDeptWmsStyle.ts
 `resolveOccupationDeptWmsStyleName` — 부서업무에서 울진 색으로 바꿔 그리던 처리를 끄고, 본표·필지 레이어 기본 색을 쓰도록 함

수정 | src/app/(pages)/map/_mapComponents/OpenLayersMap.tsx
 부서업무 전용 스타일 적용 — 끄고 실레이어만 표시

수정 | src/app/(pages)/map/_mapContents/occupationLedger/useOccupationLedgerParentGeomHighlight.ts
 본표 벡터 강조 — 지도에는 그리지 않고, 위치 맞추기용 범위만 구함

수정 | src/app/(pages)/map/_mapContents/river/usageDataAs/useUsageDataAsParentGeomHighlight.ts
 본표 벡터 강조 — 공통 점용과 같이 지도에는 그리지 않음

## 테이블 수정

해당 없음

## 테스트

1. 공통 점용대장을 부서업무로 열면 본표와 필지 레이어만 보여야 한다.
2. 울진 하천점용을 부서업무로 열면 본표와 필지 레이어만 보여야 한다.
3. 같은 도형이 두 겹으로 진하게 겹쳐 보이지 않아야 한다.
4. 부서업무를 닫으면 본표·필지 레이어가 꺼져야 한다.
5. 목록 행을 눌러도 본표가 벡터로 한 번 더 칠해지지 않아야 한다.
6. 목록 행을 누르면 해당 위치로 지도가 맞춰져야 한다.
7. 데이터조회에서 점용 레이어를 켜면 이전처럼 레이어 기본 색이어야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

목록에서 건을 고르면 점용 도형 범위가 빨간 선으로 다시 보이게 한다.

## 요약

본표·필지 레이어와 겹친다고 빼 두었던 선택 강조를 되돌렸다. 부서업무를 열면 본표·필지 레이어는 그대로 켜지고, 목록에서 한 건을 고르면 그 도형만 빨간 선으로 표시된다. 공통 점용대장과 울진 하천점용 모두 같다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/occupationLedger/useOccupationLedgerParentGeomHighlight.ts
 본표 선택 강조 — 목록·상세에서 고른 점용 도형을 빨간 선으로 다시 그림

수정 | src/app/(pages)/map/_mapContents/river/usageDataAs/useUsageDataAsParentGeomHighlight.ts
 본표 선택 강조 — 울진 하천점용 목록 선택 시 빨간 선을 다시 그림

## 테이블 수정

해당 없음

## 테스트

1. 공통 점용대장 목록에서 한 건을 고르면 해당 도형에 빨간 선이 보여야 한다.
2. 울진 하천점용 목록에서 한 건을 고르면 해당 도형에 빨간 선이 보여야 한다.
3. 부서업무를 열면 본표와 필지 레이어는 그대로 켜져 있어야 한다.
4. 부서업무를 닫으면 본표·필지 레이어와 빨간 선이 꺼져야 한다.
5. 수정 모드에서는 빨간 선택 선이 없어야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

추가 속성에서 항목을 넣을 때 속성명과 자료형(문자·숫자·날짜)을 고르게 한다.

## 요약

«속성을 추가해주세요»를 누르면 바로 빈 행을 넣지 않고, 속성명·자료형 창을 연다. 확인 후에 값 칸이 자료형에 맞게 나온다. 정의에서 고르는 추가는 그대로 둔다.

## 파일수정

수정 | src/app/(pages)/map/_mapComponents/layerExtra/LayerExtraFieldsEditor.tsx
 `openAddModal` / `submitAdd` — 추가 버튼에서 속성명·자료형 창을 열고, 이름 확인 후 빈 값 행을 붙임. 중복 이름은 안내하고 막음
 `EXTRA_DATA_TYPE_OPTIONS` / `extraTypeKind` — 문자·숫자·날짜를 고르고, 기존 문자 형은 글 칸으로 유지
 `renderValueInput` — 날짜는 날짜 칸, 숫자는 숫자 칸, 그 외는 글 칸

## 테이블 수정

해당 없음

## 테스트

1. 점용대장 상세 수정에서 «속성을 추가해주세요»를 누르면 속성명·자료형 창이 나와야 한다.
2. 속성명 없이 추가하면 안내가 나오고 행이 붙지 않아야 한다.
3. 이미 있는 속성명이면 안내가 나오고 행이 붙지 않아야 한다.
4. 날짜를 고르면 값 칸이 날짜 선택이어야 한다.
5. 숫자를 고르면 값 칸이 숫자 입력이어야 한다.
6. 문자를 고르면 값 칸이 글 입력이어야 한다.
7. 정의에서 추가는 창 없이 목록에 붙어야 한다.
8. 취소하면 행이 추가되지 않아야 한다.

## 프롬프트

brief: —
plan: docs/plan/20260902_1026_추가속성자료형.md

---

2026-09-02 | dev.strcat | —

## 목적

추가 속성 화면에서 이름 안내를 «속성명»으로 맞춘다.

## 요약

목록 칸 안내가 «필드명», 추가 창은 «속성명»이어서 섞여 보였다. 업무 화면 문구를 모두 «속성명»으로 통일했다.

## 파일수정

수정 | src/app/(pages)/map/_mapComponents/layerExtra/LayerExtraFieldsEditor.tsx
 `placeholder` — 목록 입력 안내를 추가 창과 같이 «속성명»으로 맞춤

## 테이블 수정

해당 없음

## 테스트

1. 추가 속성 목록 빈 칸 안내가 «속성명»이어야 한다.
2. 속성 추가 창의 이름 칸 안내도 «속성명»이어야 한다.

## 프롬프트

brief: —
plan: docs/plan/20260902_1026_추가속성자료형.md

---

2026-09-02 | dev.strcat | —

## 목적

개발자 모드 레이어 설정 Extra 화면의 이름 안내를 «속성명»으로 맞춘다. 개발 서버가 네이티브 드라이버를 찾아 경고를 반복하던 설정을 다시 넣는다.

## 요약

Extra 표·입력·오류 문구를 업무 화면과 같이 «속성명»으로 바꿨다. 전체 느림의 주원인은 네이티브 드라이버 경고만이 아니다. 개발 페이지가 메뉴 화면을 한 번에 묶고, 없는 심볼 그림 요청이 컴파일을 기다리며 막힌다. 네이티브 모듈은 없어도 되는데 웹팩이 매번 찾아 경고와 컴파일 부담을 더한다. 그 찾기는 다시 무시하도록 넣었다.

## 파일수정

수정 | src/app/(pages)/dev/_components/LayerExtraManager.tsx
  Extra 표·입력·오류 — «필드명»을 «속성명»으로 맞춤

수정 | next.config.ts
 `IgnorePlugin` / `pg-native` — 선택 네이티브 모듈을 웹팩이 찾지 않게 다시 무시

## 테이블 수정

해당 없음

## 테스트

1. 레이어 설정 Extra 표 머리와 입력 안내가 «속성명»이어야 한다.
2. 속성명 없이 Extra 항목을 추가하면 «속성명을 입력하세요» 안내가 나와야 한다.
3. 개발 서버를 다시 켠 뒤 네이티브 드라이버를 찾을 수 없다는 경고가 반복되지 않아야 한다.
4. 지도·개발자 모드가 이전처럼 열려야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

하천·도로·국공유지 점용대장의 본표·필지·물건지 지도 색이 서로 겹치지 않고 바로 구분되게 한다.

## 요약

기존에는 노랑·주황·파랑이 여러 레이어에 겹쳐 본표·필지·물건지를 한눈에 구별하기 어려웠다. 유형마다 다른 색 계열을 쓰고, 같은 계열 안에서도 본표·필지·물건지 색과 테두리를 다르게 맞춰 지도·범례에서 구분되도록 했다.

## 파일수정

수정 | geoserver_modules/data_dir/styles/water_occupationledger.css·sld
 `water_occupationledger` — 하천 본표를 청록 반투명·진한 청록 테두리로 맞춤

수정 | geoserver_modules/data_dir/styles/water_occupationledger_jijuk.css·sld
 `water_occupationledger_jijuk` — 하천 필지를 파랑 반투명·진한 파랑 테두리로 맞춤

수정 | geoserver_modules/data_dir/styles/water_occupationledger_mgj.css·sld
 `water_occupationledger_mgj` — 하천 물건지를 자주 반투명·진한 자주 테두리로 맞춤

수정 | geoserver_modules/data_dir/styles/road_occupationledger.css·sld
 `road_occupationledger` — 도로 본표를 주황 반투명·진한 주황 테두리로 맞춤

수정 | geoserver_modules/data_dir/styles/road_occupationledger_jijuk.css·sld
 `road_occupationledger_jijuk` — 도로 필지를 보라 반투명·진한 보라 테두리로 맞춤

수정 | geoserver_modules/data_dir/styles/road_occupationledger_mgj.css·sld
 `road_occupationledger_mgj` — 도로 물건지를 갈색 반투명·진한 갈색 테두리로 맞춤

수정 | geoserver_modules/data_dir/styles/public_occupationledger.css·sld
 `public_occupationledger` — 국공유지 본표를 남색 반투명·진한 남색 테두리로 맞춤

수정 | geoserver_modules/data_dir/styles/public_occupationledger_jijuk.css·sld
 `public_occupationledger_jijuk` — 국공유지 필지를 분홍 반투명·진한 분홍 테두리로 맞춤

수정 | geoserver_modules/data_dir/styles/public_occupationledger_mgj.css·sld
 `public_occupationledger_mgj` — 국공유지 물건지를 연두 반투명·진한 연두 테두리로 맞춤

수정 | src/lib/occupationLayerStyle.ts
 주석 — 공통점용 본표·필지·물건지는 유형별로 구분되는 색을 쓴다고 맞춤

## 테이블 수정

해당 없음

## 테스트

1. 하천점용대장을 켜면 본표는 청록, 필지는 파랑, 물건지는 자주로 보여야 한다.
2. 도로점용대장을 켜면 본표는 주황, 필지는 보라, 물건지는 갈색으로 보여야 한다.
3. 국공유지점용대장을 켜면 본표는 남색, 필지는 분홍, 물건지는 연두로 보여야 한다.
4. 같은 유형에서 본표·필지·물건지를 함께 켜도 색이 겹쳐 보이지 않아야 한다.
5. 레이어 목록 범례 색이 지도 면 색과 같아야 한다.
6. 울진 하천점용 본표·필지·물건지 색은 이전과 같아야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

점용대장 지도 테두리는 흰색으로 두고, 부서업무 목록에서 필지 레이어만 켜고 끌 수 있게 한다.

## 요약

면 색은 그대로 두고 테두리만 흰색으로 되돌렸다. 부서업무를 열 때는 본표와 필지가 함께 켜지고, 목록 점사용료 옆에 «필지» 버튼으로 필지만 끄고 켤 수 있다. 목록 행을 눌러도 필지 꺼짐 상태는 유지된다.

## 파일수정

수정 | geoserver_modules/data_dir/styles/*_occupationledger*.css·sld
 테두리 — 면 색은 유지하고 테두리만 흰색으로 복구

수정 | src/app/(pages)/map/_mapContents/occupationLedger/occupationLedgerMapSync.ts
 `ensureOccupationLedgerWmsLayers` — 부서업무 첫 진입만 필지를 켜고, 이후에는 필지 켜짐 상태를 유지. 도형 수정 중에는 본표만 숨김
 `toggleOccupationLedgerJijukWmsLayer` / `isOccupationLedgerJijukWmsVisible` — 목록에서 필지 레이어만 켜고 끔

수정 | src/app/(pages)/map/_mapContents/occupationLedger/OccupationLedgerListPanel.tsx
 «필지» 버튼 — 점사용료 옆에 두고, 켜지면 해당 유형 필지 색으로 표시

수정 | src/lib/occupationLayerStyle.ts
 `OCCUPATION_LEDGER_JIJUK_FILL` — 하천·도로·국공유지 필지 면 색. 목록 버튼 켜짐 표시에 사용

## 테이블 수정

해당 없음

## 테스트

1. 하천·도로·국공유지 점용대장을 열면 본표와 필지가 함께 켜져야 한다.
2. 목록 «필지»를 끄면 필지만 사라지고 본표는 남아야 한다.
3. 필지를 끈 뒤 목록 행을 눌러도 필지가 다시 켜지지 않아야 한다.
4. «필지»를 다시 켜면 필지가 보여야 한다.
5. 점용대장 도형 테두리는 흰색이어야 한다.
6. 점사용료 버튼은 이전처럼 켜고 꺼져야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

필지 레이어가 점용 도형 위에 덮이지 않고, 점용 도형 아래에 깔리게 한다.

## 요약

본표와 필지를 함께 켜면 필지가 이름순으로 위에 그려지던 것을, 필지를 먼저 그리도록 맞춰 점용 도형이 위에 보이게 했다.

## 파일수정

수정 | src/lib/mapLayerGeometryOrder.ts
 `WMS_STACK_ALWAYS_BOTTOM` — 하천·도로·국공유지 필지와 울진 필지를 점용 본표보다 아래에 깔리도록 고정

## 테이블 수정

해당 없음

## 테스트

1. 부서업무 점용대장을 열면 필지가 점용 도형 아래에 보여야 한다.
2. 필지를 껐다 켜도 점용 도형이 필지 위에 있어야 한다.
3. 점사용료를 함께 켜도 점용 도형은 맨 위에 있어야 한다.

## 프롬프트

brief: —
plan: —

---

2026-09-02 | dev.strcat | —

## 목적

부서업무 점용대장에서 점용 필지 레이어·버튼을 없애고, 대신 지적도가 보이게 한다.

## 요약

목록의 필지 버튼을 제거했다. 부서업무를 열면 점용 필지는 켜지 않고 지적만 켜지며, 패널을 닫으면 지적을 다시 끈다. 점용 본표는 이전처럼 표시된다.

## 파일수정

수정 | src/app/(pages)/map/_mapContents/occupationLedger/OccupationLedgerListPanel.tsx
 `OccupationLedgerListPanel` — 필지 버튼 제거. 열릴 때 지적 켜고, 닫을 때 끔

수정 | src/app/(pages)/map/_mapContents/occupationLedger/occupationLedgerMapSync.ts
 `ensureOccupationLedgerWmsLayers` — 부서업무에서 점용 필지 레이어를 켜지 않음
 `setOccupationLedgerCadastralOverlay` — 지적 컨트롤을 켜고 끄는 요청

수정 | src/app/(pages)/map/_mapComponents/OpenLayersMap.tsx
 지적 컨트롤 이벤트 — 요청한 지적 레이어만 선택에 더해 켬

수정 | src/lib/occupationLayerStyle.ts
 `OCCUPATION_LEDGER_JIJUK_FILL` — 필지 버튼용 색 상수 제거

## 테이블 수정

해당 없음

## 테스트

1. 부서업무 점용대장을 열면 목록에 필지 버튼이 없어야 한다.
2. 부서업무를 열면 점용 본표와 지적이 보여야 한다.
3. 점용 필지 색 도형은 보이지 않아야 한다.
4. 점용대장을 닫으면 지적이 꺼져야 한다.
5. 점사용료 버튼은 이전처럼 켜고 꺼져야 한다.

## 프롬프트

brief: —
plan: —

