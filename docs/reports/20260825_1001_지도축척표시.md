# 결과보고서

2026-08-25 | JungWooojin | 지도 축척 표시 요청

## 목적

2D 지도 화면에 현재 확대 수준에 맞는 축척을 보이게 한다. 기존 시스템과 같은 줌 기준 계산을 쓴다.

## 요약

오른쪽 아래에 거리 숫자와 얇은 밑선만 둔다. 줌 레벨로 `7864320 / 2^(줌-1)`을 구해 km·m·mm로 보여 준다. 줌 16이면 `240.00m`이다.

## 파일수정

수정 | src/app/(pages)/map/_mapComponents/hooks/useMapInstance.ts
 `useMapInstance` — 기본 축척막대 컨트롤 제거

추가 | src/app/(pages)/map/_mapComponents/hooks/MapScaleIndicator.tsx
 `formatScaleFromZoom` — 줌 기준 거리 계산 후 km·m·mm 표기
 `MapScaleIndicator` — 줌·이동 시 우측 하단 축척 갱신

추가 | src/app/(pages)/map/_mapComponents/hooks/mapScaleLine.css
 `축척 스타일` — 배경 박스 없이 검정 글자·밑선만

수정 | src/app/(pages)/map/_mapComponents/OpenLayersMap.tsx
 `OpenLayersMap` — 지도 위에 축척 표시 연결

## 테이블 수정

해당 없음

## 테스트

1. 지도 오른쪽 아래에 `○○○.○○m`(또는 km)과 짧은 밑선이 보여야 한다.
2. 줌 16에서 `240.00m`이 나와야 한다.
3. 지도를 확대하면 숫자가 작아져야 한다.
4. 지도를 축소하면 숫자가 커지고, 1000m 이상이면 km로 바뀌어야 한다.
5. 흰 박스·단계 눈금은 보이지 않아야 한다.
6. 오른쪽 지도 컨트롤·측정 도구 동작에 영향이 없어야 한다.

## 프롬프트

brief: 해당 없음
plan: 해당 없음
