import { ShootingRequestStandaloneClient } from '@/app/(pages)/map/_mapContents/shootingRequest/ShootingRequestStandaloneClient';

/** 촬영요청 신청서 단독 화면 — 지도·사이드바 없이 신청 모달(서식)만 */
export default function DroneRequestPage() {
  return <ShootingRequestStandaloneClient />;
}
